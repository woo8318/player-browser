<?xml version="1.0" encoding="utf-8"?>
<!--
  In-place player surface (v1.3.89). TextureView on purpose: a SurfaceView
  punches a hole through the window and ignores Compose clipping, so a video
  scrolled half under the toolbar would paint over it. `surface_type` is
  XML-only on PlayerView, hence this resource instead of a programmatic view.
-->
<androidx.media3.ui.PlayerView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:surface_type="texture_view"
    app:use_controller="false"
    app:resize_mode="fit"
    app:shutter_background_color="@android:color/black" />
