# CLAUDE.md

Player Browser — Android WebView 기반 브라우저. URL 탐색 + 동영상 제스처 컨트롤 + 내장 Media3 플레이어(스트림 추출 후 네이티브 재생, 스트림 감지 시 화면에 재생 버튼 자동 표시, 영상 롱프레스로 그 영상 메뉴→외부 플레이어 연결) + 영상 오프라인 다운로드(HLS 포함, 받아두면 재생 중 네트워크 미사용, 받으면서 바로 보기) + 동영상 이어보기 + 즐겨찾기/방문기록 + 멀티탭(썸네일 갤러리/그룹/그룹 내 새 탭/그룹순서변경/멀티선택/부모복귀/카드메뉴 그룹 이동/드래그 그룹 이동·탭 사이 재정렬/그룹 헤더 드래그 순서변경/바 스와이프 탭 전환/탭별 히스토리 영속화) + 광고 차단 + 쿠키 동의 배너 자동 거부 + SNI 우회 + 프라이빗 DNS(DoH) + URL 숫자 자동/수동 복구 + 링크 항상 새 탭 열기 + 링크 롱프레스 컨텍스트 메뉴 + 캡차 흐름 보호 + Chromecast + 자체 업데이트 + 크래시 로깅. 현재 버전: v1.3.76.

## 빌드 / 배포

- **빌드 시스템:** Gradle 8.7 + Kotlin 1.9.24 + AGP 8.5.2, JDK 17
- **타깃:** `compileSdk=34`, `minSdk=26`, `targetSdk=34`
- **앱 ID:** `com.playerbrowser.app` (debug는 `.debug` suffix)
- **버전 관리:** `app/build.gradle.kts`의 `versionCode` / `versionName` 직접 수정
- **로컬 빌드:** `./gradlew assembleDebug` → `app/build/outputs/apk/debug/app-debug.apk`
- **CI:** push 시 GitHub Actions가 debug APK 빌드, `v*` 태그 push 시 GitHub Release 생성 + 최신 3개 릴리스만 유지
- **로컬 wrapper 없음:** CI가 `gradle wrapper --gradle-version 8.7`로 생성. 로컬에서 처음 빌드할 때도 동일 명령 필요할 수 있음

## 디렉터리 구조

```
app/src/main/
  assets/video_gestures.js              # WebView에 주입되는 제스처 JS
  java/com/playerbrowser/app/
    MainActivity.kt                     # 단일 액티비티 + Compose, CastSessionBridge attach/detach
    PlayerBrowserApp.kt                 # Application — CrashRecorder.install / Proxy / SNI+AdBlock 옵저버 / Cast init
    cast/                               # Chromecast (mediarouter + play-services-cast)
      CastOptionsProvider.kt
      CastSessionBridge.kt
      VideoStreamSniffer.kt             # 페이지 내 video URL 추출 (.m3u8 / .mp4 / .webm) — 호스트별 다중 후보 보관 (current=단일 최선, all=전체, matching=DOM src 대응)
    data/                               # Room DB + 세션 영속화
      AppDatabase.kt
      Bookmark.kt / BookmarkDao.kt
      HistoryEntry.kt / HistoryDao.kt
      BrowserRepository.kt
      TabPersistence.kt                 # 멀티탭 세션 영속화 (tabs + parent + groups, JSON in SharedPreferences)
      TabWebStateStore.kt               # 탭별 WebView 뒤로/앞으로 히스토리 영속화 (saveState/restoreState Bundle → filesDir/tabstate/<id>.bin)
      WatchProgressStore.kt             # 동영상 이어보기 위치 영속화 (page URL → pos/dur/title, JSON in SharedPreferences)
    network/                            # 네트워크 인터셉트 / 진단
      AdBlocker.kt / AdBlockSwitch.kt   # 광고 차단 (host suffix + URL pattern, 빈 204 응답)
      CookieBannerKiller.kt             # 쿠키 동의 배너 자동 거부 JS (OneTrust/Cookiebot/Quantcast/Didomi/Sourcepoint/TrustArc)
      CookieBannerSwitch.kt             # 볼륨 토글 (volatile, hot path)
      DohClient.kt                      # DNS-over-HTTPS (RFC 8484 wire-format, 제공자 설정 가능, TTL 캐시 + 동시조회 합류 + 공유 클라이언트)
      DohProvider.kt                    # DoH 제공자 프리셋(Cloudflare/Google/Quad9/AdGuard) + 커스텀 URL
      PrivateDnsSwitch.kt               # 프라이빗 DNS 토글 + 현재 DoH URL (volatile, hot path)
      EnvSpoofSwitch.kt                 # JS 환경 위장 토글 (BrowserEnvPatch 주입 on/off, A/B 비교용)
      FragmentingSocketFactory.kt       # TLS ClientHello 조각화
      SniBypassClient.kt / SniBypassSwitch.kt
      ProxyManager.kt                   # HTTP/HTTPS 프록시 (WebView Proxy API)
      ResumeSwitch.kt                   # 이어보기 토글 (volatile, JS 브리지 hot path)
      UrlRecovery.kt                    # URL 숫자 자동 복구 (접속 실패 시 도메인/경로 숫자 증감 후보 프로브 → 자동 이동)
      ChallengeDetector.kt              # 캡차/안티봇 챌린지 판별(가로채기 금지) + 챌린지 호스트 격리(네이티브 전담) + 감지/루프 진단 로그
      ChallengeCookies.kt               # 격리 진입/루프 시 낡은 Cloudflare 챌린지 쿠키(cf_clearance 등) 만료 — 중복 토큰이 통과를 막던 원인
      UserAgentSpoof.kt               # WebView 기본 UA에서 `; wv` / `Version/4.0` 표식 제거 → 평범한 Chrome for Android UA + Sec-CH-UA 브랜드 정합
      CookieFlusher.kt                  # CookieManager.flush() 디바운서 (setCookie로 심은 쿠키 영속화)
      TabTitlePrefetcher.kt             # 백그라운드 탭 <title> 프리페치 (WebView 없이 OkHttp로 앞부분만 받아 파싱 → 스위처 카드 식별)
      NetworkSettings.kt / NetworkSettingsRepository.kt
      CrashRecorder.kt                  # 충돌 영속화 (filesDir/crashes/)
      DebugLog.kt                       # 인앱 로그 링버퍼 (500개)
    ui/                                 # Jetpack Compose 화면
      RootNavigation.kt                 # navigation-compose 그래프
      BrowserScreen.kt                  # 상단(주소+즐겨찾기+Cast+메뉴) + 중앙(WebView) + 하단(뒤로/앞/새로고침/홈/탭)
      BrowserWebView.kt                 # WebViewClient / WebChromeClient / GestureCapturingFrame
      BrowserViewModel.kt               # 탭 + 그룹 + 부모-자식 + isCurrentBookmarked + 업데이트 상태
      TabSwitcher.kt                    # 탭 갤러리/스위처 오버레이 (썸네일 카드 + 그룹 섹션(색상 칩+개수 배지+그룹 내 새 탭 ＋버튼) + 그룹 헤더 ⋮ 메뉴 순서 변경(위/아래) + 길게 누름 멀티 선택 + 카드 ⋮ 메뉴 1탭 이동 + 일괄 닫기/이동 + 드래그 그룹 이동·탭 사이 재정렬)
      TabThumbnailStore.kt              # 탭별 WebView 썸네일 메모리 캐시 (갤러리 카드 미리보기, 프로세스 종료 시 휘발)
      TabDragAndDrop.kt                 # 탭 카드/그룹 헤더 포인터 드래그 → 그룹 이동 + 탭 사이 재정렬 + 그룹 순서변경 (TabDragState + tabDragSource/groupDragSource/tabDropRegion/tabDragContainer — 이동·릴리스는 컨테이너 소유, 릴리스 시점 hit-test)
      TabSwipeGesture.kt                # Modifier.tabSwitchSwipe — 상/하단 바 수평 스와이프 → 인접 탭 전환
      BookmarksScreen.kt / HistoryScreen.kt
      DownloadsScreen.kt              # 다운로드 목록 (1초 폴링, 받는 중에도 탭하면 재생 — 받은 구간은 디스크·나머지는 네트워크)
      SettingsScreen.kt / SettingsViewModel.kt
      DebugLogScreen.kt                 # 로그 + 충돌 기록 뷰어 (확장/복사/삭제)
      UpdateDialog.kt
      ErrorPage.kt
      theme/
    update/                             # 자체 업데이트 (GitHub Releases)
      UpdateClient.kt / UpdateInstaller.kt / UpdateModels.kt / Version.kt
      UpdateInstallReceiver.kt          # PackageInstaller 세션 결과 수신 (확인 창 띄우기 / 실패 토스트)
    player/                             # 내장 네이티브 동영상 플레이어 (Media3/ExoPlayer)
      VideoPlayerActivity.kt            # 스니핑된 스트림을 Referer/Cookie/UA 주입해 네이티브 재생 + 제스처(더블탭 ±10초/밝기/볼륨) + 이어보기 공유 + 다운로드 캐시 읽기 + 버퍼 확대
      DownloadCenter.kt                 # 영상 오프라인 다운로드 엔진 (DownloadManager + SimpleCache/NoOpCacheEvictor, HLS는 DownloadHelper, ResolvingDataSource로 호스트별 Referer/UA + 라이브 Cookie 재부착, mediaItemFor=받는 중 재생용 스트림 키)
      VideoDownloadService.kt           # 다운로드용 포그라운드 서비스 (진행률 알림 직접 구성, Scheduler 없음)
    web/
      UrlUtils.kt                       # URL/검색어 판별 + 정규화
      WebAssetLoader.kt                 # assets/JS 로딩
      IframeScriptInjector.kt           # cross-origin iframe HTML에 JS prepend
      BrowserEnvPatch.kt                # document-start 주입 — WebView JS 환경 표식 제거(window.chrome / Notification) + 환경 진단 프로브
      ResumeBridge.kt                   # `window.PBResume` @JavascriptInterface — 이어보기 위치 save/load/clear
      PlayerBridge.kt                   # `window.PBPlayer` @JavascriptInterface — 영상 롱프레스 → 외부 플레이어 openVideo(domSrc), 메인스레드 마샬 후 콜백
```

## 핵심 아키텍처 메모

- **단일 액티비티 + Compose:** `MainActivity` → `RootNavigation` → 화면별 Composable. `MainActivity`는 `CastSessionBridge` lifecycle만 관리. **`MainActivity`는 `AppCompatActivity`(= `FragmentActivity`)여야 함(v1.3.46)** — Cast `MediaRouteButton`을 탭하면 기기 선택 다이얼로그를 `getSupportFragmentManager()`로 띄우는데, 예전처럼 `ComponentActivity`면 `IllegalStateException("The activity must be a subclass of FragmentActivity")`로 **버튼 누르는 순간 크래시**했음. 이 때문에 앱 테마 `Theme.PlayerBrowser`도 `Theme.AppCompat.Light.NoActionBar`(AppCompat 계열)로 둠(AppCompatActivity 요구 + Cast 다이얼로그 테마링). `VideoPlayerActivity`는 Cast/프래그먼트 미사용이라 `ComponentActivity` + Material 테마 그대로 둠.
- **상태 관리:** `BrowserViewModel` (탭 / 그룹 / 현재 URL / 제목 / 즐겨찾기 / 업데이트 상태), `SettingsViewModel` (네트워크 설정)
- **WebView:** `buildBrowserWebView()`에서 단건 생성. `WebViewClient.shouldInterceptRequest`가 **AdBlocker → VideoStreamSniffer → SniBypassClient → IframeScriptInjector** 순서로 체이닝. `onPageFinished`에서 `video_gestures.js` + (광고차단 켜져있으면) `AdBlocker.HIDE_CSS_JS` + (쿠키배너 켜져있으면) `CookieBannerKiller.SCRIPT` inject.
- **멀티탭:** 탭 전환 시 WebView 자체를 swap (단일 WebView 재사용 X). `SnapshotStateMap<TabId, BrowserWebViewState>`로 보관. 탭 닫히면 `webView.destroy()`로 GC.
- **탭별 히스토리 영속화 (`TabWebStateStore`, v1.3.43):** 앱을 껐다 켜도 탭 안에서 이동해 온 **뒤로/앞으로 히스토리**를 유지. 예전엔 `TabPersistence`가 탭의 **최종 URL만** 저장해서, 복원된 탭은 `load(currentUrl)`로 그 페이지만 다시 띄우고 `WebBackForwardList`가 비어 있어 Android 뒤로가기를 누르면 곧장 탭이 닫혔음(부모 없으면 back-handler가 히스토리 없음으로 판단). 이제 `WebView.saveState(Bundle)`로 각 탭의 네비게이션 리스트를 직렬화해 `filesDir/tabstate/<tabId>.bin`(마샬링한 `Bundle`, Room 회피—`TabPersistence`/`WatchProgressStore`와 같은 파일/prefs 패턴)에 저장하고, 탭 WebView를 새로 만들 때 `restoreState(Bundle)`로 되살림. **저장 시점:** `RootNavigation`이 `LifecycleEventObserver`로 `ON_STOP`(백그라운드 진입—프로세스 킬 직전 신뢰 훅)에 열린 모든 탭의 WebView를 한 번에 `saveAll`, `onDispose`에서도 파괴 직전 한 번 더 flush(모든 WebView 객체는 살아있어 detached여도 `saveState` 가능). **복원 시점:** `BrowserScreen`의 `webStates.getOrPut`에서 WebView 생성 직후 `tabWebStates.restore(id, wv)` — 성공하면(`restoreState`가 non-empty 리스트 반환) `restoreState`가 현재 엔트리를 스스로 재로드하므로 `load()`를 **건너뜀**, 실패/블롭 없음이면 종전대로 `load(currentUrl)` 폴백. **방어:** 마샬링 Parcel 바이트는 OS/WebView 업그레이드 간 안정 보장이 없어 restore 전체를 try/catch로 감싸 실패 시 손상 블롭을 삭제하고 URL 로드로 폴백(크래시/데이터 손실 없음—히스토리 리스트만 포기), 과대 블롭(>1.5MB)은 쓰기 스킵. 탭이 닫혀 WebView가 GC되면 `retain(liveIds)`가 썸네일과 lock-step으로 블롭도 prune.
- **바 스와이프 탭 전환:** `TabSwipeGesture.kt`의 `Modifier.tabSwitchSwipe`를 상단 주소창 `Surface`와 하단 내비 `Row`에 적용. `detectHorizontalDragGestures`(수평 바이어스)라 탭/세로 스크롤/텍스트필드 포커스와 충돌 안 함 — 수평 의도가 확정된 뒤에만 엔게이지. 누적 거리가 touchSlop*3을 넘는 순간 드래그당 1회만 발화(즉각 반응). `viewModel.selectAdjacentTab(forward)`가 실제 전환된 경우만 `true` 반환 → 그때만 햅틱. 좌 스와이프=다음 탭(forward), 우 스와이프=이전 탭. **이전(backward) 방향은 부모 탭 우선** — 현재 탭이 `window.open`/`target=_blank`로 열린 자식(`parentTabId` 생존)이면 인접 탭 대신 부모 탭으로 복귀(back 제스처와 동일 멘탈모델), 부모가 없으면 평탄한 `_tabs` 순서 인접 탭(양 끝 멈춤, no-wrap).
- **탭 전환 슬라이드 애니메이션:** 중앙 WebView 호스트를 `AnimatedContent(targetState = activeTabId)`로 감싸 스와이프 시 콘텐츠가 가로로 슬라이드(`slideIn/OutHorizontally`, tween 260ms). `switchDirection`(+1/-1/0) 상태로 방향 결정 — **스와이프만 애니메이션을 arm**하고 `LaunchedEffect(activeTabId)`가 매 전환 후 0으로 disarm해서 탭 닫기/스위처 선택은 무애니메이션 즉시 swap. 이유: 닫힌(=`webStates` GC로 `destroy()`된) 탭을 슬라이드 아웃하면 파괴된 WebView를 참조할 수 있음. 전환 콘텐츠는 target=현재 active면 `activeWebState`, 빠져나가는 탭은 `webStates[id]`를 **생성 없이** 조회(없으면 빈 Box)해 죽은 탭 부활/크래시 방지.
- **탭 그룹 / 부모-자식:** `TabState`에 `groupId` + `parentTabId` 필드. `TabGroup`(id, name, color)은 `BrowserViewModel._groups` StateFlow + `TabPersistence`의 JSON에 함께 저장 (Room 마이그레이션 비용 회피). 그룹 삭제 시 탭은 살아남고 `groupId`만 null로 떨어짐. **그룹 순서 = `_groups` 리스트 순서**(스위처 섹션 렌더/JSON 영속화 모두 이 순서를 따름) — 그룹 헤더 ⋮ 메뉴의 "위로 이동"/"아래로 이동"이 `viewModel.moveGroup(id, up)`으로 인접 항목을 스왑. 양 끝에서는 해당 메뉴 항목을 숨김. **그룹 내 새 탭(v1.3.47):** 그룹 헤더의 ＋버튼(개수 배지와 ⋮ 메뉴 사이)이 `onNewTab(groupId)` → `viewModel.newTab(groupId = ...)`로 그 그룹 소속 새 탭을 바로 생성(스위처 닫힘 + 새 탭 활성) — 예전엔 전체에서 만들고 그룹으로 옮겨야 했음. `newTab`의 명시적 `groupId`가 `parentTabId` 그룹 상속보다 우선.
- **target=_blank / window.open → 부모 탭 복귀:** `WebChromeClient.onCreateWindow`가 throwaway WebView로 URL만 뽑아 `WebViewCallbacks.onOpenInNewTab(url)` 콜백 → `BrowserScreen`이 `viewModel.newTab(url, parentTabId = ownerId)`로 새 탭 생성 (부모의 `groupId`도 자동 상속). 자식 탭에서 뒤로가기 → 자체 history가 없으면 `viewModel.tryReturnToParent()`로 부모 탭으로 복귀 + 자식 탭 닫기. 부모 탭이 이미 사라진 자식은 `closeTabs`에서 `parentTabId`를 null로 정리해 댕글링 방지.
- **링크를 항상 새 탭에서 열기 (설정 토글, 기본 off):** target=_blank/window.open이 아닌 **일반 링크 탭**도 현재 탭을 바꾸지 않고 새 탭에서 연다. `WebViewClient.shouldOverrideUrlLoading`이 `LinkNewTabSwitch.enabled`면 `request.isForMainFrame && hasGesture() && !isRedirect` 이고 http/https인 내비게이션을 가로채 `callbacks.onOpenInNewTab(url)`(부모 탭 자식으로 생성 — 위 메모와 동일, 뒤로 가기로 복귀)로 넘기고 `true` 반환. 게이트 조건이 핵심 — 주소창 입력(`loadUrl`은 이 콜백을 안 거침)·서버 리다이렉트·제스처 없는 JS 내비게이션은 제자리 로드 유지. 설정값은 `NetworkSettings.openLinksInNewTab` → `LinkNewTabSwitch` volatile 스위치(`shouldOverrideUrlLoading` hot path라 DataStore suspend 회피, `PlayerBrowserApp` 옵저버가 미러).
- **링크 롱프레스 컨텍스트 메뉴 (v1.3.49):** 페이지의 링크를 길게 누르면 "새 탭에서 열기 / 백그라운드 탭으로 열기 / 링크 주소 복사" AlertDialog가 뜬다 — 전역 "항상 새 탭 열기" 토글을 켜고 끄는 불편 없이 링크별로 새 탭을 열 수 있는 표준 브라우저 방식. `buildBrowserWebView`의 WebView `setOnLongClickListener`가 `getHitTestResult()`로 링크 여부 판정(`SRC_ANCHOR_TYPE`/`SRC_IMAGE_ANCHOR_TYPE`만 소비, 나머지는 `false` 반환해 텍스트 선택 등 기본 동작 유지). 앵커 href는 `requestFocusNodeHref`(이미지-앵커는 `extra`가 이미지 src라 노드 href를 따로 조회)로 얻고 실패 시 `result.extra` 폴백, http/https만 메뉴 노출. "새 탭에서 열기"는 기존 `callbacks.onOpenInNewTab`(부모 탭 자식으로 생성+전환), **"백그라운드 탭으로 열기"는 새 `onOpenInBackgroundTab`** → `viewModel.newTab(url, parentTabId, activate = false)`로 현재 페이지에 머문 채 자식 탭만 뒤에 쌓음(WebView는 나중에 전환 시 lazy 생성돼 `currentUrl` 로드). `WebViewCallbacks.onOpenInBackgroundTab`은 인터페이스 기본 구현이 `onOpenInNewTab`으로 위임(팝업 WebView 등 미구현 구현체 안전). AlertDialog는 AppCompat 계열이라 `MainActivity`(AppCompatActivity) 컨텍스트에서 정상 테마링. **백그라운드 탭 타이틀 프리페치(v1.3.53):** 백그라운드 탭은 WebView를 늦게(전환 시) 만들므로 그때까진 제목이 없어 스위처 카드가 호스트("youtube.com")만 보여 여러 개를 열면 뭐가 뭔지 구분이 안 됐음(사용자 피드백). 이제 `BrowserViewModel.newTab(activate=false)`이 `prefetchBackgroundTitle`로 `TabTitlePrefetcher.fetchTitle(url, WebView 기본 UA)`를 백그라운드 실행 — OkHttp로 페이지 앞부분(≤128KB)만 받아 `<title>`을 파싱해 `updateTab`으로 `currentTitle` 채움(카드가 즉시 recompose, `TabPersistence`에 함께 저장돼 재시작에도 유지). 라우팅은 WebView와 동일하게 미러(SNI 우회 켜짐→ClientHello 단편화+DoH, 프라이빗 DNS만→평문+DoH, 둘 다 꺼짐→평문+시스템 DNS)라 DPI 차단 사이트도 동일하게 도달. Content-Type/`<meta charset>`로 인코딩 감지(EUC-KR 등 한국 사이트 대비). best-effort — 실패 시 null 반환해 종전 호스트 플레이스홀더 유지. **탭 활성화(전환)는 안 함** — 사용자가 좋아한 "백그라운드에 쌓기"는 그대로 두고 식별성만 보강. 전환 후 WebView `onPageFinished`의 실제 제목이 프리페치 값을 덮어씀(`currentTitle.isBlank()` 가드로 프리페치는 아직 안 채워진 경우만 기록).
- **탭 스위처 멀티 선택 + 카드 메뉴 1탭 이동:** `TabSwitcher.kt`의 `TabSwitcherOverlay`(internal). `combinedClickable`(ExperimentalFoundationApi) 길게 누름 → `selectedIds` Set에 추가 → `derivedStateOf`로 선택 모드 진입. 선택 모드에서는 상단 액션 바가 morph(전체 / 그룹으로 이동 / 일괄 닫기). BackHandler가 선택 모드면 선택 해제, 아니면 오버레이 dismiss. 그룹별 섹션은 `LazyVerticalGrid` + `GridItemSpan(maxLineSpan)` 헤더로 렌더. 단일 탭 이동은 카드 우측 ⋮ 메뉴 ("그룹으로 이동..." / "그룹에서 빼기") 로 멀티 선택 없이 처리 — `pendingMoveTargets: List<String>?` 상태가 단일/멀티 양쪽 흐름을 통일해 `GroupPickerDialog`/`NewGroupDialog`(create-then-move 원자 처리) 한 벌을 공유. 카드 메뉴는 `inSelectMode == false` 일 때만 노출해 선택 모드 액션과 충돌하지 않음. **열 때 활성 탭으로 자동 스크롤** — 오버레이는 열릴 때마다 새로 composition되므로 `LaunchedEffect(Unit)`에서 `activeTabLazyIndex()`(섹션 헤더 span + 탭 카드 emit 순서를 그대로 미러링해 활성 탭의 평탄 grid 인덱스 계산)로 위치를 구해 `gridState.scrollToItem`. 보던 탭이 목록 아래쪽에 있어도 바로 보임.
- **탭 갤러리 썸네일 (Opera/삼성인터넷 스타일):** 탭 카드가 URL 텍스트 대신 실제 페이지 썸네일을 보여준다. `TabThumbnailStore`(`ui/`)가 탭 id → `ImageBitmap` 메모리 캐시(`mutableStateMapOf`라 새 캡처가 들어오면 카드가 즉시 recompose). 캡처는 `webView.draw(Canvas(downscaled bitmap))`로 현재 뷰포트를 600px 폭 RGB_565로 다운스케일(투명 페이지 대비 흰 배경 fill) — **활성 탭만 attach/레이아웃되어 있으므로 그 순간만 신뢰성 있게 캡처 가능**. 그래서 `BrowserScreen`의 `captureActive()`가 (1) 탭 스위처 열기 직전(`TabCountButton` onClick), (2) 바 스와이프 탭 전환 직전(`switchAdjacentTab`)에 현재 활성 WebView를 캡처한다. 아직 캡처 안 된 탭은 `hostOf(url)` 도메인 플레이스홀더 카드. 저장소는 webStates와 함께 `RootNavigation`에 remember되어 네비게이션을 살아남고, WebView GC `LaunchedEffect`에서 `thumbnails.retain(liveIds)`로 동시 prune(프로세스 종료 시 휘발 — 영속화 안 함, 재방문 시 재캡처). `TabCard`는 상단 제목 바(활성 탭은 primaryContainer 틴트) + 하단 썸네일 `Image(ContentScale.Crop, TopCenter)` 구조, 활성 탭은 강조 테두리(2.5dp primary)+그림자(6dp).
- **드래그로 탭 그룹 이동 + 탭 사이 재정렬 (`TabDragAndDrop.kt`, v1.3.44 자체 포인터 드래그로 재작성):** 카드 롱프레스 → (기존대로 선택 모드 진입) → 손을 떼지 않고 touch slop 이상 끌면 드래그 시작. **이전 플랫폼 drag-and-drop(`dragAndDropSource`/`dragAndDropTarget`)은 실기기에서 드롭이 안 먹혔음**(사용자 피드백 "드래그는 되는데 down이 안돼 — 탭 사이에 넣거나 다른 그룹으로 이동이 안됨"). Compose 1.6.x가 `onDrop`을 불안정하게 전달하고 `startTransfer`가 조용히 no-op 되는 제약 때문. 그래서 **플랫폼 DnD를 완전히 걷어내고 자체 포인터 드래그로 교체**: `tabDragSource`가 `detectDragGesturesAfterLongPress`로 손가락 위치를 카드의 `boundsInRoot().topLeft` 기준 root 좌표로 변환해 `TabDragState`에 실시간 주입, 각 헤더/카드는 `tabDropRegion`(`onGloballyPositioned`)으로 자기 root bounds를 `TabDragState.regions`에 등록. **드롭은 릴리스 순간(`onDragEnd`) `dragState.resolve()`가 마지막 포인터로 regions를 hit-test**해서 결정 — 플랫폼 왕복이 없어 매번 착지하고, 카드 위 hit이면 그 카드 기준(좌/우 절반 = before/after)으로 **탭 사이 삽입(재정렬)**, 헤더/빈 섹션 hit이면 그 그룹으로 이동. 단일 카드가 다른 카드 위면 `onMoveTab(tabId, anchor, placeAfter, group)`(재정렬), 그 외(멀티선택·헤더·빈 섹션)는 `onMoveToGroup`. 재정렬은 `BrowserViewModel.moveTab`이 평탄 `_tabs` 리스트에서 위치를 옮겨 구현(스위처가 그룹별로 `_tabs` 순서대로 렌더하므로 리스트 재배치 = 시각적 재정렬), `combine(_tabs,_activeTabId,_groups).onEach{save}`로 자동 영속화. 드래그 중: 손가락을 따라다니는 플로팅 배지(`DragPreview`, 소스 카드는 제자리서 40% dim만), hover 카드에 삽입 위치 표시 바(`InsertSide` BEFORE/AFTER, `drawWithContent`로 좌/우 가장자리), 빈 "그룹 없음" 섹션 항상 노출(그룹에서 빼기 타깃), 가장자리 64dp 밴드 자동 스크롤. 선택된 카드를 끌면 `selectedIds` 전체 이동. `tabDragSource`는 반드시 `combinedClickable` **뒤에** 체이닝 — Main 포인터 패스에서 안쪽 노드가 먼저 이벤트를 소비해야 clickable의 long-press 후 `consumeUntilUp`이 드래그를 죽이지 못함. **드래그 이동·릴리스는 컨테이너 소유(v1.3.48 버그 수정):** 예전엔 카드의 `detectDragGesturesAfterLongPress`가 세션 전체(이동/드롭)를 소유했는데, 카드는 lazy 아이템이라 **가장자리 자동 스크롤로 소스 카드가 뷰포트 밖으로 나가면 LazyGrid가 dispose → 포인터 코루틴 사망 → 드래그가 중간에 끊겨** 하단 탭을 위쪽 그룹으로 못 올렸음(사용자 피드백). 이제 `tabDragSource`/`groupDragSource`는 롱프레스로 **세션 시작만** 하고(`begin`/`beginGroup`, end/cancel은 no-op), 그리드 호스트 Box의 `tabDragContainer`(`pointerInput` + `awaitEachGesture`)가 세션 활성화 순간부터 **Initial 패스에서 모든 이벤트를 소비**하며 moveTo/드롭/reset을 전담 — 컨테이너는 dispose되지 않으므로 소스 아이템이 죽어도 드래그가 손을 뗄 때까지 유지되고, Initial 소비가 그리드 스크롤러블의 제스처 가로채기도 차단. **그룹 헤더 드래그 순서변경(v1.3.48):** 그룹 헤더 롱프레스 드래그 → 다른 그룹 헤더 상/하 절반(=before/after, 헤더에 상/하 삽입 바 표시) 또는 그 그룹 카드(=after) 또는 "그룹 없음" 섹션(=맨 끝)에 드롭 → `BrowserViewModel.reorderGroup(id, anchorGroupId, placeAfter)`가 `_groups` 리스트를 재배치(임의 위치 — 인접 스왑인 기존 ⋮ 메뉴 `moveGroup`과 병존). 그룹 드래그 중엔 카드 삽입 바 억제(`hoverTabId=null`), 배지는 그룹 이름 표시, 셀프 드롭은 no-op.
- **URL 숫자 자동 복구 (`UrlRecovery`):** 메인 프레임이 접속 실패(`onReceivedError`, 코드 `-1/-2/-6/-7/-8/-11/-16`)하면 URL의 숫자를 증감한 후보(`+1~+10`, `-1~-3`)를 만들어 백그라운드로 접속 가능 여부를 확인하고 살아있는 가장 가까운 주소로 **자동 이동**한다. 우선순위는 **도메인 숫자**(newtoki123 → newtoki124, IP 리터럴 제외) → 없으면 경로 숫자 → 쿼리 숫자. 프로브는 WebView와 같은 경로(SNI 우회 켜짐 → DoH + ClientHello 단편화, 꺼짐 → 일반)로 OkHttp GET을 날려 응답 코드로 판정(`2xx/3xx` + `401/403/429`는 생존, `404/410/5xx`·연결에러는 사망). 프로브 동안 `ErrorPage.probing()` 대기 화면, 못 찾으면 평소 `ErrorPage.build()`. 오실레이션/폭주 방지로 최근 자동 이동 후보(60s)는 후보에서 제외하고 2분 내 자동 이동은 5회로 제한. 404/5xx 같은 HTTP 에러(`onReceivedHttpError`)는 대상이 아님 — "접속 자체가 안 되는" 경우만. 후보 생성/프로브 코어는 `buildCandidates`/`reachable`로 분리돼 자동(`candidates`+`probe`)과 수동(`findAlternative`)이 공유.
- **URL 숫자 수동 복구:** 자동 복구는 *접속 실패* 에러일 때만 발동하므로, 페이지가 정상 로드됐지만(예: 404 안내·잘못된 콘텐츠가 200으로 뜸) 사용자가 직접 새 주소를 찾고 싶을 때를 위한 수동 트리거. 상단 ⋮ 메뉴 → "주소 복구 (URL 찾기)"가 현재 URL로 `UrlRecovery.findAlternative()`를 호출 → 첫 생존 후보로 **즉시 이동**(자동과 동일 정책). 자동과 달리 **TTL/자동이동 예산 가드를 거치지 않는다**(사용자가 명시적으로 다시 시도하는 흐름). 진행/결과는 Toast로 안내, 콜백은 백그라운드 스레드이므로 `WebView.post`로 UI 복귀.
- **캡차("사람인지 확인") 흐름 보호 (`ChallengeDetector`, v1.3.54):** 체크를 해도 확인 화면만 무한 반복되던 문제. 원인은 우리가 챌린지 흐름에 끼어든 것 — (1) `IframeScriptInjector`가 **모든** cross-origin iframe 문서를 자체 OkHttp로 다시 받아 CSP를 벗기고 제스처 스크립트를 끼워 넣고 본문을 재인코딩하는데, Turnstile/hCaptcha/reCAPTCHA 위젯이 정확히 그 "cross-origin iframe 문서"라 매번 걸렸다(문서를 받은 커넥션과 그 안 JS가 이후 날리는 검증 요청의 컨텍스트가 어긋남 + `followRedirects=true`로 302가 뭉개짐 + `Set-Cookie`를 비동기 `setCookie`로 우회). (2) SNI 우회/프라이빗 DNS가 켜지면 `SniBypassClient`가 챌린지 GET은 OkHttp(DoH+단편화)로 가져오지만 **검증 POST는 `shouldInterceptRequest`가 본문을 볼 수 없어** 네이티브(시스템 DNS+평문 ClientHello)로 나가 경로가 갈라졌다. 이제 `ChallengeDetector.isChallengeRequest`(호스트 suffix: `challenges.cloudflare.com`/`hcaptcha.com`/`recaptcha.net`/`arkoselabs.com`/`geetest.com`/`datadome.co`/`perimeterx.net`/`incapsula.com` … + 경로: `/cdn-cgi/challenge-platform/`·`/cdn-cgi/managed/`·`/cdn-cgi/bm/`·`/recaptcha/`·`/turnstile/`·`/_incapsula_resource` …)에 걸리는 요청은 **양쪽 모두 손대지 않고** WebView 네이티브 로더에 통째로 맡긴다 — 챌린지 GET/POST가 같은 경로·같은 커넥션 풀을 쓰게 하는 것이 핵심. `/cdn-cgi/` **전체**를 빼지 않은 건 `/cdn-cgi/image/`(Cloudflare 이미지 리사이징)가 실제 콘텐츠 이미지라 차단 사이트에서 v1.3.39 서브리소스 수정이 되돌아가기 때문. 덧붙여 `LinkNewTabSwitch` 게이트에 `method == "GET"`을 추가 — 챌린지 통과 직후의 폼 전송(POST)이 새 탭으로 넘어가면 본문 없는 GET으로 다시 열려 캡차가 처음부터 다시 떴다.
- **캡차 호스트 격리 — 챌린지 사이트는 통째로 네이티브 전담 (v1.3.56):** v1.3.54가 챌린지 *요청*은 안 건드리게 했지만 루프가 남았다(yadongtv101.com). 남은 조각은 **챌린지 페이지 문서 자체**였다 — Cloudflare managed challenge는 사이트 루트(`https://호스트/`)가 **403 + 챌린지 HTML**로 내려오는데, 이 URL은 호스트도 경로도 `ChallengeDetector` 목록에 안 걸려 여전히 우리 OkHttp가 가져갔다. 반면 챌린지를 실제로 푸는 검증 POST는 `shouldInterceptRequest`가 본문을 못 봐 **영원히** 네이티브로 나간다. 즉 *챌린지를 푸는 건 네이티브(Chromium TLS 지문)*, *통과 후 문서를 다시 받는 건 우리 OkHttp(OkHttp 지문 + Chrome UA)*로 갈렸다. Cloudflare의 `cf_clearance`는 **IP + UA + TLS 지문(JA3)**에 묶여 발급되므로 지문이 어긋나면 무효 → 또 챌린지 → 무한 루프(같은 쿠키가 서로 다른 지문에서 오는 것 자체가 강한 봇 신호이기도 하다). POST를 가로챌 방법이 없는 이상 **일관성을 얻는 유일한 길은 그 호스트를 통째로 네이티브에 맡기는 것**이다. 이제 `ChallengeDetector`가 호스트 격리 셋(`markChallengedHost`/`isQuarantinedHost`, TTL 6시간=사실상 세션)을 들고, `SniBypassClient.intercept`와 `IframeScriptInjector.process`가 격리 호스트의 **모든** 요청(메인 프레임 문서 포함)에서 손을 뗀다. 격리 진입은 두 경로 — (1) `SniBypassClient`가 받은 **메인 프레임 응답**이 `isChallengeResponse`(403/503/429 + `cf-mitigated` 헤더, 없으면 `cf-ray`)면 응답을 버리고 `null` 반환해 WebView가 처음부터 네이티브로 다시 요청하게 함(그 순간 `cf_clearance` 보유 여부도 로그), (2) `probe`의 DOM 감지가 **가로막는 인터스티셜**일 때(제목이 "Just a moment"/"사람인지" 류 이거나 `#challenge-form` 존재) — 로그인 폼에 얹힌 reCAPTCHA 위젯 정도로는 격리하지 않는다(멀쩡한 사이트가 SNI 우회를 잃으면 손해가 더 크다). **자가 치유:** 네이티브 경로엔 ClientHello 단편화가 없어 DPI 차단 사이트는 접속 자체가 안 될 수 있으므로, `onReceivedError`가 접속 실패성 코드(`UrlRecovery.shouldProbe`)를 보면 `clearQuarantine(host)`으로 격리를 풀어 다음 시도는 다시 우회 경로로 나간다(캡차 루프보다 접속 불가가 더 나쁘다).
- **캡차 루프의 마지막 조각 — 낡은 통과 쿠키 + 페이지 개입 (v1.3.57):** v1.3.56으로 격리는 걸렸는데(`챌린지 요청 통과(sni-격리)`) yadongtv101.com이 여전히 루프였다. 로그가 `감지 #6`(14:53:45) → `#7`(14:54:03)로 18초 간격이었다는 게 결정적 — **챌린지는 통과되고 페이지가 재로드됐는데 곧바로 다시 챌린지**, 즉 새로 발급된 `cf_clearance`가 거부되고 있었다. 원인 둘. **(1) 우리가 심어놓은 낡은 쿠키(`ChallengeCookies`).** v1.3.54~55의 인터셉터가 `Set-Cookie`를 떼어 `CookieManager.setCookie()`로 직접 심고 `CookieFlusher`로 **디스크에 영속화**했는데, 그렇게 심은 쿠키는 도메인 스코프가 네이티브 로더가 심는 것과 어긋날 수 있어(`example.com` vs `.example.com`) 같은 이름의 `cf_clearance`가 **두 벌** 남는다. 그러면 이후 모든 요청에 통과 토큰이 두 개 실려 Cloudflare가 거부하고, 몇 번을 풀어도 새 토큰이 낡은 토큰과 함께 나가며 영원히 다시 챌린지가 뜬다(다른 브라우저는 이 오염이 없으니 같은 사이트가 잘 열린다). 이제 격리 진입 시 + 루프 감지(3회 이상, 호스트당 60초 1회) 시 `ChallengeCookies.reset(host)`이 그 호스트의 `cf_clearance`/`__cf_bm`/`__cfruid`/`cf_chl*`를 있을 법한 도메인 스코프 전부에 대해 만료시켜 깨끗한 상태로 다시 시작하게 한다(사이트 자체 로그인 쿠키는 안 건드림). **(2) 챌린지 페이지에 대한 개입.** 격리 호스트는 `shouldInterceptRequest`에서 **광고차단까지 포함해** 응답을 만드는 단계 전체를 건너뛰고(빈 204도 챌린지 스크립트에겐 로드 실패다 — 단 `VideoStreamSniffer.observe`는 관찰만 하므로 유지, 안 그러면 격리된 동영상 사이트에서 재생 버튼이 영영 안 뜬다), 챌린지 화면이 떠 있는 동안은 `onPageFinished`의 제스처 JS·광고차단 CSS·쿠키배너 킬러 주입과 `window.PBResume`/`window.PBPlayer` 브리지 노출도 모두 접는다(네이티브 브리지는 페이지가 `window`를 훑으면 그대로 보여 자동화 신호가 된다). **두 개의 창을 분리한다** — 네트워크 격리는 TTL 6시간(통과 토큰이 네이티브 지문에 묶여 있으므로 통과 후에도 유지), 페이지 기능 억제는 `isChallengeActive` 60초 창(감지될 때마다 갱신, 통과 감지 시 즉시 해제)이라 챌린지만 지나면 그 사이트에서 제스처·이어보기·플레이어가 정상 동작한다.
- **캡차 루프의 진짜 마지막 조각 — 우리가 WebView라는 사실 (`UserAgentSpoof`, v1.3.58):** v1.3.56(호스트 격리) + v1.3.57(낡은 쿠키 정리)로 **네트워크 경로는 오페라/크롬과 동일하게** 만들었는데도 yadongtv101.com이 그대로 루프였다. 격리가 걸리면 문서·서브리소스·검증 POST가 전부 WebView 네이티브 Chromium 스택으로 나가므로 TLS 지문(JA3)·HTTP/2 지문·IP는 이미 오페라와 같다. 남은 차이는 **브라우저 환경 그 자체**였고, 그중 가장 크게 드러나 있던 것이 **User-Agent**다. Android WebView의 기본 UA는 `Mozilla/5.0 (Linux; Android 14; SM-S926N Build/UP1A.231005.007; **wv**) … **Version/4.0** Chrome/120.0.0.0 Mobile Safari/537.36` 형태로, `; wv`와 `Version/4.0`이 "나는 인앱 WebView다"라고 스스로 밝히는 표식이다(진짜 Chrome UA엔 둘 다 없다). Cloudflare는 보안 등급이 높은 존에서 WebView/인앱 브라우저에 **통과 점수를 아예 주지 않기 때문에**, 체크박스를 눌러 챌린지를 풀어도 환경 점수에서 떨어져 새로 발급된 `cf_clearance`가 즉시 무효가 되고 다시 챌린지가 뜬다 — "풀리긴 하는데 곧바로 다시 뜬다"(감지 #6→#7이 18초 간격)는 로그와 정확히 일치한다. 이제 `UserAgentSpoof.chromeLike()`가 기본 UA에서 **표식만 걷어내** 같은 기기의 실제 Chrome UA와 형태를 맞춘다(Android 버전·기기명·Chrome 빌드번호는 그대로 두고 `Build/`도 제거 — Chrome UA엔 없고 기기 빌드 지문도 안 흘림). 예전에 UA 뒤에 임의 접미사를 붙였다가 `ERR_CONNECTION_RESET`을 맞았던 것과는 **반대 방향(비표준 → 표준)** 이라 위험이 낮다. 결과가 `Chrome/`·`Mozilla/5.0`을 잃으면 원본 UA를 그대로 쓰는 안전장치가 있고, 정규화 전/후 UA를 디버그 로그에 한 번 남긴다. **한계:** UA-CH(`Sec-CH-UA`)는 여전히 "Android WebView"로 나가고 `window.chrome`도 없다 — 이보다 더 강하게 위장하려면 페이지에 스크립트를 주입해야 하는데, 그건 v1.3.54~57에서 루프의 원인이었던 "챌린지 페이지 개입"을 되살리는 짓이라 하지 않는다.
- **헤더가 아니라 JS 환경이 문제였다 (`BrowserEnvPatch`, v1.3.60):** v1.3.59 로그로 두 가지가 더 정리됐다. (1) `Sec-CH-UA 브랜드 → Chromium/Google Chrome 150`이 찍혀 **UA-CH 정합은 실제로 적용됐고**, 그래도 챌린지는 감지 #1~#5로 그대로 반복됐다 — 헤더 수준의 WebView 표식은 이제 없다. (2) `window.PBResume`/`window.PBPlayer` 브리지 노출 가설도 **로그가 반증했다** — `challengeActive`는 감지 #1에 켜지므로 로드 #2의 `onPageStarted`에서 `removeJavascriptInterface`가 불리고(반영은 다음 로드부터) 로드 #3부터는 브리지가 없는데, #3·#4·#5도 똑같이 실패했다. 그래서 남은 것은 **자바스크립트로 읽히는 브라우저 환경**뿐이다. 진짜 Chrome for Android엔 있고 WebView엔 없는 대표적 표식 둘 — `window.chrome`(`loadTimes()`/`csi()`, 자동화 판별의 고전적 지표)과 `Notification`(WebView엔 아예 정의돼 있지 않음) — 을 `WebViewCompat.addDocumentStartJavaScript`(androidx.webkit 1.11.0, `WebViewFeature.DOCUMENT_START_SCRIPT` 가드)로 **페이지 첫 스크립트보다 먼저** 채운다. **이것은 v1.3.54~57에서 금지한 "챌린지 페이지 개입"과 다르다** — 그때 루프를 만든 것은 `shouldInterceptRequest`로 문서를 OkHttp로 다시 받아 CSP를 벗기고 재인코딩해 **네트워크 경로가 갈라진 것**이었고, document-start 주입은 네트워크를 전혀 건드리지 않고 WebView 자체 로더가 만든 문서에 코드만 얹는다. 방향도 반대다(신호를 더하는 게 아니라 지운다). 모든 오리진에 건다 — 챌린지 페이지에서만 환경이 달라지면 그 차이 자체가 신호가 되고, 안티봇은 챌린지 이전 페이지에서도 지문을 수집한다. **이미 값이 있으면 손대지 않고**(진짜 값을 우리가 덮어써 어긋나게 만들지 않음) 전체를 try/catch로 감싼다. 셰이프를 어설프게 흉내 내면 그 자체가 신호이므로 널리 검증된 둘만 손대고 `PaymentRequest` 부재 같은 나머지 표식은 남겨둔다.
- **실기기 오페라 대조 진단 페이지 + 알림 권한 질의 버그 수정 (v1.3.73):** v1.3.72까지 JS 환경/UA/쿠키/네트워크를 전부 추측으로 훑었으니, 이제 **추측 대신 실측**으로 바꾼다. `debug-tools/fingerprint-scan.html`(앱 빌드에는 안 들어감, `raw.githack.com`으로 GitHub 원본을 `text/html`로 서빙 — jsDelivr/statically.io는 `.html`을 보안상 `text/plain`으로 강제해 브라우저가 렌더링을 안 함, 유일하게 원본 MIME을 그대로 주는 게 raw.githack.com)이 UA/UA-CH/자동화 표식/화면/그래픽 지문/권한 상태를 한 화면에 모아 raw text로 뽑아준다. 같은 URL을 챌린지를 **통과하는** 오페라와 **루프하는** Player Browser 양쪽에서 열어 대조한 결과, `navigator.permissions.query({name:'notifications'})`가 오페라는 `prompt`, 우리는 `denied`였다. `BrowserEnvPatch`(v1.3.63)의 기존 패치는 "이 질의가 **거부(reject)**된다"고 가정하고 `catch`만 잡았는데, 실제 이 WebView 버전(150)은 거부하지 않고 **정상 resolve하면서 state:'denied'를 준다** — 그래서 패치가 한 번도 발동하지 않았다. `resolve`된 값도 함께 확인해 `denied`면 `prompt`로 맞추도록 고쳤다. (같은 대조에서 `window.outerWidth/Height`도 오페라 대비 확연히 달랐다 — 오페라는 outer/inner 차이가 168px(주소창+내비 영역)인데 우리는 1px. 다만 앱 내장 WebView는 구조적으로 "브라우저 UI"가 없는 뷰라 그런 것이고, 숫자를 억지로 지어내면 오히려 더 강한 신호가 될 수 있어 **손대지 않았다**.) **이 수정도 루프 자체를 고친다는 보장은 없다** — v1.3.72가 이미 확인했듯 JS 환경을 통째로 꺼도 켜도 루프가 동일했으므로, 진짜 원인은 JS 레벨보다 WebView 네트워크/TLS 엔진 자체일 가능성이 높다. 그래도 실측으로 찾은 진짜 버그이니 고치고 다음 테스트 사이클로 넘긴다.
- **순정 WebView도 똑같이 루프한다 — 원인은 우리 코드가 아니다 + 깨끗한 시작 (v1.3.72):** v1.3.71 로그가 결론을 냈다. `브라우저 위장 꺼짐 — 순정 WebView UA/헤더/JS 그대로` 상태(= 아무것도 안 건드린 기본 WebView)에서도 감지 #7·#8로 **똑같이** 루프했고, SNI 우회 false/true 양쪽, 토큰 삭제 중단(v1.3.71) 상태에서도 그대로였다. `cf_clearance`는 매 라운드 새 값(`FVeRZe5XfK…`→`fRtMJnvaLN…`→`sOhkIJQX.n…`→`AeNicnQxRd…`, len=597)으로 발급되고 다음 요청에서 거부된다. **v1.3.58~71이 의심한 모든 것(UA·Sec-CH-UA·JS 환경·쿠키 삭제·네트워크 경로)이 배제됐다** — Cloudflare가 이 존에서 Android WebView 자체에 통과 점수를 안 주는 것이고, 우리 쪽에서 지울 수 있는 표식은 남아 있지 않다. 다만 한 번도 시도된 적 없는 상태가 하나 있다 — **완전히 깨끗한 시작**. v1.3.69에서 격리를 디스크에 영속화하면서 `markChallengedHost(preflight = true)`(낡은 챌린지 상태를 청소하던 유일한 지점)가 다시는 돌지 않게 됐고, 그래서 한 번 루프에 빠진 호스트는 앱을 껐다 켜도 낡은 `cf_clearance` / `cf_chl_rc_ni`(non-interactive 챌린지 재시도 카운터)를 그대로 들고 다시 시작한다. 그래서 ⋮ 메뉴에 **"이 사이트 데이터 지우고 새로고침"** 을 넣는다 — `ChallengeCookies.resetSite(host)`(그 호스트의 **모든** 쿠키 만료, 사용자가 직접 부를 때만) + `ChallengeDetector.forgetHost(host)`(격리·감지 횟수·챌린지 창·쿨다운 전부 해제) + `WebView.clearCache(true)` 후 재로드. 브라우저라면 당연히 있어야 할 기능이면서, 캡차 루프를 백지 상태에서 한 번 시험하는 유일한 길이다. **자동 경로에서는 절대 쓰지 않는다** — 사이트 로그인 세션까지 함께 날아간다.
- **위장 토글이 "위장 전부"를 끄게 한다 + 통과 토큰을 더 이상 지우지 않는다 (v1.3.71):** v1.3.70 로그로 A/B 토글이 실제로 동작하게 됐는데도 루프가 그대로였다. 그런데 그 A/B 자체가 반쪽이었다 — 토글은 `BrowserEnvPatch`(document-start JS 주입)만 껐고 **UA 정규화(v1.3.58)와 `Sec-CH-UA` 브랜드 교체(v1.3.59)는 그대로 적용**됐다. 즉 "끄고 테스트"가 순정 WebView 테스트가 아니었다. 이제 `BrowserWebView`의 `settings.apply` 에서 `EnvSpoofSwitch.enabled` 하나가 **UA·Sec-CH-UA·`X-Requested-With`·JS 환경 네 가지 전부**를 감싼다 — 끄면 아무것도 손대지 않은 기본 WebView 그대로 나가고(로그: `브라우저 위장 꺼짐 — 순정 WebView UA/헤더/JS 그대로`), 설정 화면 이름도 "브라우저 위장"으로 바꿨다. 켠 쪽에는 아직 한 번도 안 건드린 표식 하나를 추가한다 — WebView가 모든 요청에 붙이는 `X-Requested-With: com.playerbrowser.app`(진짜 Chrome은 안 보냄, JS 환경 진단으로는 보이지 않아 v1.3.58~70이 계속 놓쳤다)를 `WebSettingsCompat.setRequestedWithHeaderOriginAllowList(settings, emptySet())`로 제거하고, 적용 전 목록 크기를 함께 찍어 **이 기기가 원래 그 헤더를 보내고 있었는지**를 로그로 확정한다(최신 WebView는 이미 기본 미전송일 수 있다). 함께 **루프 중 `cf_clearance` 삭제를 중단**한다 — v1.3.57이 이 삭제를 넣은 근거("우리가 심은 토큰이 스코프 중복")는 v1.3.56 호스트 격리 이후 **우리가 그 호스트에 쿠키를 심는 경로 자체가 없어** 구조적으로 불가능하고 로그도 내내 `cf_clearance=1`이었다. 반대로 루프 판정(3회)은 챌린지가 도는 중에 내려지므로 방금 발급된 새 토큰을 우리가 지워 다음 요청을 확실히 실패시키는 모양이었고, v1.3.69에서 `Secure` 스코프 버그를 고치면서 이 삭제가 **처음으로 실제로 먹히기 시작**했다. 이제 지우지 않고 관찰만 한다(챌린지 **시작 전**의 `resetAll`은 그대로 — 그 순간엔 진행 중인 흐름도 새 토큰도 없다).
- **A/B 토글이 켜지지도 않고 있었다 (v1.3.70):** 사용자가 "JS 환경 위장 끄고 테스트한 것" 이라고 했는데 로그엔 `JS 환경 정규화 주입: window.chrome + …` 이 그대로 찍혔다(껐다면 `JS 환경 위장 꺼짐(설정) — document-start 주입 생략` 이 나와야 한다). 즉 **v1.3.67 의 A/B 는 한 번도 실제로 수행된 적이 없다.** 원인은 경쟁 조건 — 설정값은 DataStore 에 있고 `PlayerBrowserApp.observeNetworkSwitches()` 가 **비동기로** `EnvSpoofSwitch` 에 미러하는데, 다른 스위치와 달리 이 값만 **WebView 를 만드는 순간**(`BrowserEnvPatch.install`) 읽힌다. 로그상 UA 정규화 5ms 뒤에 이미 주입이 끝나 있어 DataStore 의 첫 emission(디스크 읽기)이 항상 진다 → 재시작하면 기본값 `true` 로 주입됐다. 이제 이 한 값만 **동기적으로 읽히는 SharedPreferences(`env_spoof`)에 미러**한다 — `NetworkSettingsRepository.update` 가 저장할 때 `EnvSpoofSwitch.persist` 로 함께 쓰고, `PlayerBrowserApp.onCreate` 의 `EnvSpoofSwitch.attach()` 가 WebView 가 만들어지기 전에 먼저 올린다(로그에 `JS 환경 위장 설정 로드: 켬/끔`). DataStore 가 계속 원본(설정 화면이 보여주는 값)이고 여기 미러는 사본이다. **교훈:** WebView 생성 시점에 읽히는 값은 요청 hot path 스위치와 달리 비동기 미러를 기다릴 수 없다.
- **격리 이전의 첫 요청 한 번이 점수를 깎고 있었다 (v1.3.69):** v1.3.68 로그가 갈림길을 갈랐다 — `cf_clearance` 토큰이 **매 라운드 새 값**(`3CAHkpUscS…` → `VFo.2dVTPw…` → `uY8Z6OKiJl…` → `MlDnfQQHlR…`, 모두 len=597)이다. 즉 **챌린지는 매번 실제로 통과되어 새 토큰이 발급되는데 바로 다음 요청에서 서버가 그 토큰을 거부**한다("제출 실패"가 아니라 "발급 후 즉시 무효"). `cf_clearance` 는 (IP, UA, TLS 지문)에 묶여 발급되므로 남은 용의자는 **그 조합의 평판 점수**다. 그리고 로그 맨 앞에 그 점수를 깎을 만한 것이 그대로 찍혀 있다 — `intercept[sni]: GET yadongtv103.com (main frame)` + `FragOut.write: TLS record split` 이 격리가 걸리기 **직전에 한 번** 나간다. 이 요청은 UA 로는 Chrome 150 이라고 말하면서 TLS 지문은 OkHttp+단편화라, 안티봇이 가장 확실하게 잡는 불일치다. 격리(`quarantined`)가 **메모리에만** 있고 TTL 이 6시간이었던 탓에 앱을 껐다 켤 때마다 이 첫 요청이 되풀이됐다. 이제 격리 목록을 `SharedPreferences("challenge_quarantine")` 에 `host|만료시각` 으로 남기고 `PlayerBrowserApp.onCreate` 의 `ChallengeDetector.attach()` 가 **크래시 레코더 직후** 복원한다 — 첫 페이지 로드부터 격리가 적용돼 우회 경로로 새지 않는다. TTL 도 7일로 늘렸다(챌린지를 쓰는 사이트는 계속 쓴다). 접속 실패 시 격리를 푸는 자가 치유(`clearQuarantine`)는 그대로이고, 해제/만료도 디스크에 반영된다. 함께 `ChallengeCookies` 의 만료 문자열에 `Secure` 를 추가했다 — v1.3.68 로그에서 삭제 직후에도 같은 토큰이 그대로 읽혔는데, `Secure` 로 심긴 원본을 비-Secure 쿠키로는 못 덮어쓰기 때문이다.
- **업데이트 확인 HTTP 403 — GitHub API 요청 한도 (v1.3.68):** "업데이트" 를 누르면 `업데이트 오류 HTTP 403` 이 뜨던 문제. 헤더 문제가 아니라 **미인증 GitHub API 의 IP 당 시간당 60회 한도**다 — 모바일은 캐리어 CGNAT 로 IP 를 공유하므로 내가 몇 번 안 눌러도 남이 쓴 몫까지 합산돼 걸리고, 걸리면 GitHub 은 429 가 아니라 **403 + `X-RateLimit-Remaining: 0`** 을 준다. 이제 `UpdateClient` 가 403/429 를 받으면 **API 를 아예 안 타는 경로로 폴백**한다 — `github.com/<owner>/<repo>/releases/latest` 는 일반 웹 페이지라 한도와 무관하고, 리다이렉트를 따라가지 않고 `Location`(`.../releases/tag/v1.3.68`)에서 최신 태그만 뽑은 뒤 APK 주소를 CI 의 이름 규칙(`player-browser-<tag>.apk`, `.github/workflows/android.yml` 의 `cp … "release-artifacts/player-browser-${GITHUB_REF_NAME}.apk"`)으로 조립한다. 릴리스 노트와 파일 크기는 못 가져오지만(진행률은 다운로드 응답의 `Content-Length` 로 잡힌다) **업데이트 자체는 그대로 된다.** 폴백까지 실패하면 `X-RateLimit-Reset` 으로 "약 N분 뒤 다시 시도할 수 있습니다" 를 알려준다 — 벌거벗은 `HTTP 403` 보다 낫다. 함께 **한도를 덜 쓴다**: 앱을 켤 때마다 돌던 조용한 자동 확인(`BrowserScreen` 의 `checkForUpdates(silent = true)`)을 6시간에 한 번으로 묶었다(`SharedPreferences("update_check")`). 사용자가 직접 누르는 확인은 종전대로 언제나 나간다.
- **더한 것이 해였는지 처음으로 확인한다 (`EnvSpoofSwitch`, v1.3.67):** v1.3.65 로그가 두 가지를 확정했다. (1) `JS 환경 진단 — 없음=[-]` — 이 기기에서 **결손 API가 하나도 남지 않았고** `maskShare=true, maskToString=true` 로 `Function.prototype.toString` 마스킹까지 실제로 먹고 있다. (2) 그런데도 `메인 프레임 HTTP 403: yadongtv101.com/ [cf-mitigated=challenge]` 가 **매 라운드 예외 없이** 찍힌다 — 200 이 한 번도 없다. 즉 v1.3.58~63 이 목표한 "표식 지우기"는 **완수됐는데 결과는 그대로**다. 그렇다면 한 번도 확인하지 않은 가정이 하나 남는다 — **더한 것 자체가 신호일 가능성**. `Function.prototype.toString` 이 네이티브가 아닌 함수로 교체된 것은 그 자체가 자동화 지표이고, 안티봇은 새 iframe(다른 realm)의 원본과 대조하면 즉시 알아챈다. Turnstile 은 정확히 그런 cross-realm 검사를 한다. 그래서 **설정 → "JS 환경 위장" 토글**(기본 켬)로 `BrowserEnvPatch.install` 을 통째로 건너뛸 수 있게 해 사용자가 직접 A/B 비교하게 한다(WebView 생성 시점에 읽으므로 새 탭부터 적용). 추측으로 한쪽을 고르는 대신 **끄고 켜서 답을 얻는다.**
- **갈림길을 가르는 쿠키 지문 (v1.3.67):** 로그의 `쿠키 2개, cf_clearance=1` 은 개수만 알려줘서 정작 중요한 걸 못 봤다 — **그 토큰이 라운드마다 새것인가.** 값이 매번 **바뀌면** 챌린지는 실제로 풀려 새 `cf_clearance` 가 발급되는데 서버가 다음 요청에서 곧바로 거부하는 것(= 환경 점수)이고, **그대로면** 애초에 새 토큰이 발급되지 않는 것(= 챌린지 제출 자체가 실패)이라 원인이 완전히 다르다. `ChallengeCookies.describe` 가 토큰 앞 10자와 길이를 함께 찍고(`토큰=abc1234567…(len=182)`, 전체는 남기지 않음), **매 감지 라운드마다** 호출한다 — 예전엔 격리 진입과 루프 확정(3회) 때만 찍어 중간 라운드가 비어 있었다.

- **추측 대신 진단 — 이 기기에 실제로 뭐가 빠져 있는가 (v1.3.61):** WebView에 없는 API 목록은 기기/WebView 버전마다 다르고, 추측으로 셰이프를 흉내 내면 어설픈 모조품이 오히려 더 강한 신호가 된다. 그래서 `BrowserEnvPatch.probeEnvironment`가 앱 실행당 1회(챌린지가 아닌 페이지의 `onPageFinished`) 진짜 Chrome for Android엔 있는 API 25개를 훑어 **없는 것만** `DebugLog`에 남긴다 — `PaymentRequest`/`PublicKeyCredential`/`navigator.credentials`/`speechSynthesis`/`navigator.getInstalledRelatedApps` 등. 덧붙여 `navigator.plugins.length`, `navigator.webdriver`, `navigator.userAgentData.brands`(= v1.3.59의 UA-CH 정합이 JS 쪽에도 반영됐는지), 그리고 `window`에 남아 있는 `PB*` 브리지 이름을 함께 찍는다. 다음 라운드에서 무엇을 채울지 **로그가 직접 알려주게** 하는 것이 목적이다.
- **챌린지 구간의 로그 사각지대 제거 (v1.3.64, 진단 전용):** 루프 로그를 다시 읽다 보니 감지 #9(16:24:52.6)와 다음 요청(16:25:07.1) 사이 **14.5초 동안 로그가 완전히 비어 있었다** — 정작 챌린지가 돌아가는 구간이 통째로 사각지대. 원인은 `noteSkipped`가 (종류, 호스트)당 30초에 한 번만 기록하도록 스로틀돼 있어서(챌린지는 요청이 수십 개씩 쏟아지므로 원래는 맞는 정책), 격리 호스트의 요청이 첫 한 번 뒤로는 전부 묻혔던 것. 이제 `anyChallengeActive()`가 참인 동안(= 어느 호스트든 챌린지 창 60초가 열려 있는 동안)에만 **경로별 3초 창**으로 전환해 챌린지 진행 중 무엇을 받아가는지 다 남기고, 챌린지가 없을 때는 종전대로 조용하다. 함께 `WebViewClient.onReceivedHttpError`를 처음으로 오버라이드해 **메인 프레임 응답 코드와 `cf-*` 헤더**를 기록한다(`ChallengeDetector.noteHttpError`) — 격리 호스트는 우리 OkHttp를 전혀 안 거쳐 응답을 볼 방법이 없었는데, 매 라운드가 `403 (cf-mitigated=challenge)`로 계속 거부되는지 아니면 한 번 200으로 통과했다가 다음 요청에서 다시 떨어지는지가 원인 판별의 갈림길이다. **동작은 아무것도 바꾸지 않는다**(`onReceivedHttpError` 기본 구현은 no-op, 로그만 추가).
- **shim이 가짜라는 게 그대로 보이던 구멍 (v1.3.63):** v1.3.62로 9개를 채웠는데도 yadongtv101.com 루프가 감지 #11까지 이어졌다(쿠키는 `cf_clearance=1`로 여전히 깨끗 — 오염이 아니라 서버가 토큰을 거부하는 것). 코드를 다시 보니 **채우는 방식 자체에 구멍**이 있었다. `nativeLike`가 shim 함수에 **자체 `toString`**을 심었는데, 안티봇이 실제로 쓰는 검사는 `Function.prototype.toString.call(fn)` 형태라 own 프로퍼티를 건너뛰고 원본을 호출한다 → `function share() {}`가 그대로 노출됐다. 즉 v1.3.62는 "없음"을 **"명백한 가짜가 있음"**으로 바꿔 놓은 셈이고, native 함수엔 없는 own `toString` 프로퍼티가 남는 것 자체도 표식이었다. 이제 `Function.prototype.toString`을 **한 번만** 프록시해 우리가 만든 함수(`WeakSet` 등록분)에만 `[native code]`를 돌려주고 나머지는 원본에 그대로 위임한다 — 함수 소스를 읽는 라이브러리(DI 컨테이너 등)는 영향을 안 받고, `Function.prototype.toString.toString()`도 native로 보이도록 프록시 자신을 등록해 둔다. 함께 두 가지를 더 맞춘다: (1) `window.outerWidth/outerHeight`가 **0일 때만** inner 값으로 교정(헤드리스 판별의 고전 지표, 값이 멀쩡하면 손대지 않음), (2) v1.3.60이 `Notification`을 채우면서 생긴 모순 — `navigator.permissions.query({name:'notifications'})`가 WebView에선 거부되어 "Notification은 있는데 권한 질의는 실패"라는 정직한 부재보다 나쁜 조합이 됐던 것 — 을 'prompt'로 맞춘다(원본을 먼저 호출하고 실패했을 때만 대체). **진단도 값 기반으로 확장**: `probeEnvironment`가 존재 여부만이 아니라 `outer`/`inner`/`screen`/`dpr`/`touch`/`cores`/`mem`/`lang`/`tz`/`pdf`/`dispMode`/`notifPerm`/`chromeKeys`/`uaPlatform`/`webgl` 값과, **마스킹이 실제로 먹히는지**(`maskShare`/`maskToString` = `Function.prototype.toString.call(...)`에 `[native code]`가 들어 있는지)를 함께 찍는다 — 셰이프가 어긋난 항목은 부재보다 강한 신호라 무엇이 어긋났는지 로그가 직접 말하게 한다.
- **진단이 찍어준 9개를 그대로 채운다 (v1.3.62):** v1.3.61 진단이 이 기기(SM-F776N / WebView Chrome 150)에서 없는 API 이름을 정확히 뱉었다 — `PaymentRequest`, `PublicKeyCredential`, `navigator.mediaSession`, `navigator.share`, `navigator.bluetooth`, `navigator.usb`, `navigator.presentation`, `navigator.getInstalledRelatedApps`, `speechSynthesis`. 아홉 개 전부 Chrome for Android엔 있으므로 **하나만 없어도 WebView로 특정된다** → 골라 채우는 건 의미가 없어 `BrowserEnvPatch.SCRIPT`에 한 번에 정의한다(반대로 `plugins=0`/`webdriver=false`는 Chrome Android와 이미 같고, `window.chrome`·`Notification`·`userAgentData.brands`는 v1.3.59~60이 채운 것이 진단에서 확인됐다). **부작용을 안 만드는 방식이 핵심** — 존재는 하되 capability 질의(`canShare`/`canMakePayment`/`getAvailability`/`isUserVerifyingPlatformAuthenticatorAvailable`)는 전부 false를 돌려주므로 사이트가 "공유"·"결제"·"생체인증" 버튼을 새로 그렸다가 눌러도 아무 일 없는 상황이 안 생기고, 실제로 못 쓰는 기능은 종전처럼 조용히 숨겨진다. `navigator.*`는 인스턴스가 아니라 `Navigator.prototype`에 정의해 `Object.getOwnPropertyNames(Navigator.prototype)` 수준의 검사와도 어긋나지 않게 하고, 각 항목은 `if (name in obj)` 가드 + 개별 try/catch라 이미 있는 환경은 절대 덮어쓰지 않는다. **아직 안 한 것:** `Function.prototype.toString` 자체를 프록시하지는 않는다(섀도잉한 `toString`만 `[native code]`를 돌려줌) — 함수 소스를 들여다보는 라이브러리를 깨뜨릴 수 있어 다음 단계로 미뤄둠.
- **비대화형 챌린지 — 사용자가 누를 체크박스는 없다 + UA-CH 정합 (v1.3.59):** v1.3.58 로그가 두 가지를 확정했다. (1) `쿠키 상태: 쿠키 2개, cf_clearance=1` — **스코프 중복 오염은 없었다.** v1.3.57의 전제(통과 토큰 두 벌)는 틀렸고, 원인은 쿠키가 아니라 환경 점수다. (2) 쿠키 목록에 `cf_chl_rc_ni`가 있다 — 접미사 `_ni`가 **non-interactive**, 즉 Cloudflare가 내린 것은 **체크박스가 없는 자동 판정 챌린지**다. 화면엔 스피너만 돌고 사용자가 눌러서 풀 수 있는 요소가 애초에 없다("오페라에선 자동으로 눌러지고 통과된다"는 것이 정확히 이 자동 판정을 통과한다는 뜻). 그래서 **남은 레버는 환경 지문뿐**이고, v1.3.58이 UA 문자열만 고친 탓에 오히려 **모순**이 생겼다 — UA는 `Chrome/150`이라고 하는데 UA Client Hints(`Sec-CH-UA`)는 여전히 `"Android WebView";v="150"`을 보낸다. 두 값의 불일치는 정직한 WebView보다 **더 강한 봇 신호**다. 이제 `UserAgentSpoof.applyClientHints()`가 `WebSettingsCompat.setUserAgentMetadata`(androidx.webkit 1.11.0, `WebViewFeature.USER_AGENT_METADATA` 가드)로 브랜드 목록만 `Chromium`/`Google Chrome`/GREASE 조합으로 갈아끼워 UA 문자열과 짝을 맞춘다 — 플랫폼·기기·버전 등 나머지 필드는 기존 메타데이터를 복사해 그대로 두므로 UA와 계속 일치하고, 미지원 기기에선 조용히 넘어간다(그 WebView는 `Sec-CH-UA` 자체를 안 보내 불일치도 없다). **구조적 한계는 인정한다:** `window.chrome` 부재·빈 `navigator.plugins`·`Notification` 미지원 등 JS 주입 없이는 못 지우는 WebView 표식이 남아 있고, 챌린지 페이지에 스크립트를 주입하는 것은 v1.3.54~57 루프의 원인이라 **하지 않는다**. 그래서 루프가 3회로 확정되면 `ChallengeDetector.notifyStuck`이 **토스트로 사용자에게 직접 안내**한다 — ⋮ 메뉴의 기존 "외부 브라우저로 열기"가 확실한 우회 수단이다(호스트당 60초 1회). 무한 스피너만 도는 것보다 막다른 길임을 알려주는 편이 낫다.
- **v1.3.57 쿠키 삭제의 타이밍 버그 수정 (v1.3.58):** v1.3.57의 `ChallengeCookies.reset`은 `cf_clearance`뿐 아니라 `__cf_bm`/`cf_chl*`까지 한꺼번에 지웠는데, 뒤의 둘은 **지금 돌고 있는 챌린지의 진행 상태**다. 그런데 삭제가 불리던 시점 중 둘(=`onPageFinished` DOM 인터스티셜 감지, 루프 감지)은 **챌린지 화면이 이미 떠 있는 순간**이라, 우리가 진행 중인 흐름을 스스로 끊어 통과 자체를 불가능하게 만들고 있었다. 이제 삭제를 두 단계로 나눈다 — `resetAll`(통과 토큰 + 진행 상태)은 `SniBypassClient`가 403을 버리고 네이티브로 재요청시키는 **챌린지 시작 전** 단 한 곳(`markChallengedHost(preflight = true)`)에서만, 그 외 흐름이 도는 중에는 `resetClearance`(이미 거부된 낡은 통과 토큰만)만 부른다. 덧붙여 격리 진입/루프 감지 로그에 `ChallengeCookies.describe(host)`로 **쿠키 실태**(총 개수, `cf_clearance` 개수, cf 계열 이름)를 함께 남겨 "스코프 중복으로 통과 토큰이 두 벌"인지 한 줄로 확인 가능하게 했다 — `cf_clearance=2` 면 오염 확정, `=1`인데도 루프면 원인은 쿠키가 아니라 환경 점수(위 UA 항목).
- **캡차 통과 쿠키 영속화 (`CookieFlusher`, v1.3.54):** 우리 인터셉터가 `Set-Cookie`를 응답 헤더에서 떼어 `CookieManager.setCookie()`로 직접 심는데(`SniBypassClient`/`IframeScriptInjector`), WebView는 이렇게 들어온 쿠키를 바로 디스크에 쓰지 않아 프로세스가 죽으면 `cf_clearance` 같은 통과 쿠키가 사라졌다(= 앱 켤 때마다 캡차 재시도). `Set-Cookie`를 본 응답마다 `CookieFlusher.schedule()`(1.5초 디바운스, 데몬 스레드)로 flush를 예약하고, `RootNavigation`의 `ON_STOP`에서 `flushNow()`로 확정.
- **캡차 진단 로그 (v1.3.54):** 어떤 사이트가 어떤 캡차를 쓰는지 사용자가 알 수 없으므로 자가진단을 넣음. `onPageFinished`마다 `ChallengeDetector.probe(view, url)`가 `PROBE_JS`(브리지 없이 `evaluateJavascript` 콜백으로 결과 회수)로 DOM에서 알려진 위젯(`.cf-turnstile`/`#challenge-form`/`.g-recaptcha`/`.h-captcha`/arkose·geetest·datadome iframe + "Just a moment"·"사람인지" 류 제목)을 찾아 `DebugLog`에 `Captcha` 태그로 남긴다. 위젯이 있는데 CSS로 숨겨졌으면 `(hidden!)`을 붙여 광고차단/쿠키배너 CSS가 캡차를 가린 경우를 구분. 쿼리를 뗀 `host+path`로 반복 횟수를 세어 **3회 이상이면 "루프 의심"을 ERROR로 승격**하고, 감지 시 토글 스냅샷(SNI우회/프라이빗DNS/광고차단/쿠키배너제거/링크새탭)을 함께 기록해 원인 격리를 돕는다. 캡차가 사라지면 카운터를 지우고 "통과"를 남김. 가로채기를 포기한 챌린지 요청도 (종류, 호스트)당 30초 1회로 로그.
- **iframe 처리:** `IframeScriptInjector`가 cross-origin iframe HTML 응답에 `<script>`를 prepend해 제스처 JS 주입.
- **광고 차단 (`AdBlocker`):** 약 50개 광고/트래커 host suffix + URL 패턴 매칭으로 빈 204 반환. 메인 프레임은 절대 차단 안 함. CSS 셀렉터로 same-domain 광고 슬롯 숨김. `AdBlockSwitch.enabled` volatile 플래그 (hot path).
- **쿠키 동의 배너 자동 거부 (`CookieBannerKiller`):** OneTrust / Cookiebot / Quantcast(TCF) / Didomi / Sourcepoint / TrustArc / Google 컨센트의 "Reject All" 버튼 셀렉터 리스트를 들고 400ms 간격으로 최대 10회 폴링 클릭. 동시에 컨센트 컨테이너를 `display:none` + `html,body{overflow:auto}`로 CSS 숨김 (배너 dismiss 실패 시 콘텐츠/스크롤 락 같이 풀어주는 fallback). `window.__pbCookieKill` 플래그로 idempotent — 매 `onPageFinished`에서 재주입돼도 1회만 동작. `CookieBannerSwitch.enabled` volatile 플래그 (hot path).
- **SNI 우회:** `SniBypassClient` + `FragmentingSocketFactory`가 TLS 레코드 레이어에서 ClientHello를 분할. `DohClient`로 DNS도 우회. `SniBypassSwitch.enabled` volatile 플래그. **OkHttp 클라이언트는 `(fragment, followRedirects)` 2축 매트릭스로 4개** — `fragment`는 SNI 우회 켜짐일 때만 `FragmentingSocketFactory` 부착(프라이빗 DNS 단독이면 평문 소켓, 불필요한 단편화의 호환성 리스크 회피), `followRedirects`는 메인 프레임=false(OkHttp가 내부적으로 리다이렉트를 따라가면 WebView 주소창 URL/origin과 어긋남, 메인 3xx는 WebView가 처리)·서브리소스=true. **WebView는 가로챈 *서브리소스* 응답의 리다이렉트를 따라가지 않으므로**, 같은 호스트 이미지/CSS/JS가 301/302(서명 CDN URL·http→https·확장자 리라이트 등)로 응답하면 3xx를 그대로 돌려줄 경우 조용히 로드 실패 → 서브리소스 클라이언트가 리다이렉트를 직접 따라가 네이티브 로더처럼 로드되게 함. **서브리소스 게이트(v1.3.39):** SNI 우회가 켜지면 `bypassedHosts`(같은 호스트)뿐 아니라 **모든 https 서브리소스**를 가로채 단편화한다 — 웹툰/이미지처럼 본문과 *다른 호스트의 CDN*(예: blacktoon\*.com 페이지가 `img.koimagemoa.com`/`webimg7.com`에서 이미지 로드)이 그 자체로 DPI 차단(ClientHello SNI 보고 TLS RST)되는데, 이들은 메인 프레임이 아니라 `bypassedHosts`에 절대 안 들어가 예전엔 네이티브 경로로 빠져 이미지가 통째로 안 떴음. 이제 단편화 경로로 돌려 해결(OkHttp가 못 가져오면 null 반환 → 네이티브 폴백이라 안전). **단, cross-host 확장 경로는 `Range` 헤더(미디어)면 가로채지 않고 네이티브로 흘림** — 동영상 탐색/점진 재생은 네이티브 로더의 206 처리·스트리밍 중 복원력에 의존(헤더 커밋 후엔 우리 스트림 래퍼가 못 따라감). 이미지/CSS/JS는 Range가 없어 그대로 수정 적용. same-host(`bypassedHosts`) 서브리소스는 종전대로(Range 포함) — 차단 사이트 동영상이 이 경로로 이미 동작. 프라이빗 DNS 단독이면 cross-host는 건드리지 않음(DNS는 단편화 불필요). 서브리소스 클라이언트는 더 큰 커넥션 풀(8/30s, 메인 프레임 4/5s) + `Dispatcher`(maxRequestsPerHost=16, 기본 5)로 단일 CDN 호스트 이미지 버스트 병렬성 보존.
- **프라이빗 DNS (DoH):** `DohClient`가 RFC 8484 wire-format(POST `application/dns-message`)으로 DNS를 조회해 ISP DNS를 우회/암호화. JSON API가 아니라 wire-format이라 **임의의 DoH 엔드포인트**(Cloudflare/Google/Quad9/AdGuard 프리셋 + 커스텀 URL, NextDNS 등) 지원. 제공자는 `DohProvider` enum(키→URL)으로 모델링, 설정값은 `NetworkSettings.dohProvider`/`dohCustomUrl` → `PrivateDnsSwitch.dohUrl` volatile 미러(provider 변경이 클라이언트 재생성 없이 즉시 반영, `DohClient`가 매 조회 시 live로 읽음). **적용 범위(독립 토글):** `PrivateDnsSwitch.enabled`가 켜지면 SNI 우회와 무관하게 `SniBypassClient.intercept`가 모든 https 메인 프레임을 OkHttp(평문 소켓+DoH)로 라우팅 → 선택 DNS가 모든 페이지에 적용. 인터셉트 게이트는 `if (!sni && !privateDns) return null`, 클라이언트 선택은 `fragment = sni`. **Android 제약:** WebView 네이티브 네트워킹의 DNS는 앱이 못 바꾸므로 DoH는 우리가 가로채는 요청(메인 프레임 + 그 호스트의 same-host 서브리소스, `bypassedHosts`)에만 적용 — 다른 호스트(CDN) 서브리소스는 시스템 DNS. DoH 제공자 설정은 SNI 우회 경로의 DNS에도 공유됨(기본 Cloudflare = 기존 동작 보존).
- **DoH 조회 캐시 / 첫 로드 실패 개선 (v1.3.55):** blacktoon 같은 차단 사이트가 "한 번에 안 열리고 새로고침하면 열리던" 증상의 원인 두 가지를 제거. **(1) `DohClient` 캐시.** OkHttp는 *커넥션을 새로 열 때마다* `Dns.lookup`을 부르는데 예전 구현은 캐시가 없어 매번 DoH POST를 **2번(A→AAAA 순차)** 새로 날렸다. 게다가 `DohClient()`를 만드는 6곳(`SniBypassClient` 4 + `UrlRecovery` + `TabTitlePrefetcher`)이 각자 `OkHttpClient`(=각자 커넥션 풀)를 들어 리졸버로의 TLS 핸드셰이크도 계속 새로 했다. 웹툰 페이지처럼 이미지가 한 CDN 호스트로 몰리면 서브리소스 클라이언트의 `maxRequestsPerHost=16` 때문에 **DoH 요청 32개가 동시 폭주** → 리졸버가 밀리고 → 밀린 만큼 로드가 늦어지거나 타임아웃 → `intercept`가 null 반환 → WebView가 **단편화 없는 네이티브 로더**로 떨어져 DPI에 그대로 막혔다. 게다가 DNS는 connect 이전 단계라 **OkHttp `connectTimeout(6s)`의 지배를 받지 않는다** — 5s+5s 타임아웃 × 2질의 = 최악 20초를 DNS에만 쓸 수 있었다. 이제 (a) 응답의 실제 TTL을 60s~10분으로 클램프해 `ConcurrentHashMap` 캐시(엔트리에 엔드포인트 URL을 넣어 제공자 변경 시 자동 무효화, 256개 넘으면 통째로 비움), (b) 호스트별 락으로 **동시 조회 합류** — 32-요청 폭주가 1회 조회로 접힘, (c) 리졸버용 `OkHttpClient` 한 벌 공유(`ConnectionPool(2, 5분)`)로 keep-alive 유지, (d) 타임아웃 3s + **A 질의가 실패하면 AAAA 생략**(엔드포인트가 죽은 상태)으로 ~3초 만에 시스템 DNS 폴백하고 그 폴백 결과도 60s 캐시해 커넥션마다 죽은 엔드포인트를 다시 두드리지 않음. **(2) 메인 프레임 1회 재시도.** ClientHello 단편화가 DPI를 뚫는지는 확률적(미들박스 재조립 타이밍/경로에 따라 갈림)이라, 한 번의 `IOException`으로 null을 돌려주면 WebView가 단편화 없는 네이티브 경로로 재시도해 그대로 차단당한다. `SniBypassClient.execute()`가 **메인 프레임만** 새 `Call`로 즉시 한 번 더 시도(서브리소스는 재시도 안 함 — 한 CDN에 이미지 수십 개가 몰리는 구간이라 재시도가 곱해지면 오히려 밀린다). 부수 효과로 전이적 실패에 `UrlRecovery`가 오발동해 다른 번호 도메인으로 튀는 일도 줄어든다. 메인 프레임 3xx는 `Location`을 디버그 로그에 남겨(우린 리다이렉트를 안 따라가고 WebView에 넘기므로) "빈 화면으로 멈춤"이 여기서 끊긴 건지 바로 확인 가능.
- **Chromecast:** `CastSessionBridge`를 액티비티 lifecycle에 attach/detach. 세션 시작 시 `VideoStreamSniffer.current(host)`로 현재 페이지의 캐스트 가능한 URL 조회 → `RemoteMediaClient.load()`.
- **내장 Media3 플레이어 (`player/VideoPlayerActivity`, v1.3.42):** 사이트 플레이어 위 오버레이/레이아웃과의 제스처 충돌을 근본적으로 피하려고, 페이지에서 **스트림 URL만 뽑아 별도 네이티브 플레이어로 재생**하는 경로. 상단 ⋮ 메뉴 "플레이어로 재생" **또는 화면에 자동 표시되는 플로팅 재생 버튼**이 `VideoStreamSniffer.current(host)`로 스니핑된 후보(`.m3u8`/`.mp4`/`.webm`)를 가져와 `VideoPlayerActivity.start(url, referer=현재페이지, cookie=CookieManager, ua=WebView UA, mime, title)`로 전용 액티비티를 띄움 (둘 다 `BrowserScreen`의 공용 `launchPlayer` 람다 사용).
  - **화면 내 재생 버튼 (v1.3.45):** 메뉴를 열어야 하는 불편을 없애려고, 현재 페이지에서 재생 가능한 스트림이 감지되면 WebView 우하단에 "▶ 플레이어로 재생" 플로팅 알약 버튼을 자동 노출. `VideoStreamSniffer`가 캡처할 때마다 bump하는 `revision: StateFlow<Int>`를 `BrowserScreen`이 `collectAsState`로 구독 → `remember(revision, currentUrl){ current(host) != null }`로 버튼 표시 여부를 반응형 계산(스니핑은 서브리소스 로드 중 백그라운드 스레드에서 일어나므로 snapshot state 대신 스레드-세이프 `StateFlow`). 버튼은 우측 × 로 닫을 수 있고(`playerButtonDismissed`), 매 내비게이션(`LaunchedEffect(currentUrl)`)마다 다시 노출. `AnimatedVisibility`(fade+scale)로 스트림 감지/페이지 이동 시 부드럽게 등장/퇴장.
  - **영상 롱프레스 → 그 영상 메뉴 → 외부 플레이어 연결 (v1.3.50, v1.3.51 메뉴화):** 화면 내 재생 버튼은 페이지당 하나라 **여러 영상이 있으면 어떤 영상을 여는지 알 수 없다**는 피드백을 해결. 페이지의 `<video>`를 **길게 누르면(550ms)** 바로 그 영상을 외부 Media3 플레이어로 연결. `video_gestures.js`의 `initExternalPlayer` 모듈이 롱프레스를 감지(터치 이동 12px 넘으면 취소해 스크럽/스와이프와 충돌 방지)해 그 `<video>`의 소스(`currentSrc`→`src`→`<source src>`)를 읽어 `window.PBPlayer.openVideo(domSrc)`(`PlayerBridge`) 호출. `PlayerBridge`가 메인스레드로 마샬 → `WebViewCallbacks.onPlayVideoExternally` → `BrowserScreen`이 `VideoStreamSniffer.matching(host, domSrc)`로 해석: **domSrc가 직접 미디어 URL이면 그 URL을 그대로 재생**(정확히 그 영상), `blob:`/MSE라 URL이 없으면 `current(host)`(스니핑된 네트워크 스트림)로 폴백. `VideoStreamSniffer`는 이를 위해 호스트별 **다중 후보**를 보관(`ConcurrentHashMap<host, CopyOnWriteArrayList<StreamCandidate>>`, 최대 12, `current`=단일 최선 HLS우선, `all`=전체, `matching`=domSrc 대응). **cross-origin iframe 플레이어**는 `window.PBPlayer` 브리지를 못 보므로, 영상을 가진 프레임이 소스를 `window.parent.postMessage({__pbOpenVideo})`로 상위에 릴레이하고 각 프레임이 브리지가 있는 최상위까지 재릴레이(풀스크린 `__pbFs` 릴레이와 동일 패턴). 재생/외부연결 컨텍스트(Referer/Cookie/UA)는 활성 탭 것 사용 — 활성 탭만 터치 가능하므로 안전. **v1.3.51 — 즉시 실행 → 메뉴:** 롱프레스가 **확인 없이 바로** 플레이어를 띄우고(오작동성 실행) 스트림을 못 뽑는 영상에서도 깨진 화면이 떴다는 피드백으로, 이제 `LaunchedEffect(externalPlayRequest)`가 후보를 **먼저 해석**해서 (1) 후보 있으면 `showVideoContextMenu`(AppCompat AlertDialog, 링크 롱프레스 메뉴와 동일 패턴)로 **"외부 플레이어로 재생 / 영상 주소 복사 / 취소"** 메뉴를 띄우고 사용자가 고른 뒤에만 `playCandidate`, (2) 후보가 아예 없으면(=`blob:`+스니핑 스트림도 없음) 메뉴 대신 "스트림 못 찾음" 토스트만 — 재생 불가 영상엔 쓸데없는 메뉴가 안 뜸. JS의 '여는 중…' 토스트도 제거(메뉴/토스트가 네이티브에서 피드백 담당). **v1.3.52 — 더블탭이 롱프레스 메뉴를 오발화하던 버그 수정:** `initExternalPlayer`의 롱프레스 취소 리스너(touchend/move/cancel의 `clearLp`)를 예전엔 `document` 캡처에 걸었는데, 메인 제스처 핸들러의 `document` touchend 리스너(더 먼저 등록됨)가 **더블탭 인식 시 `stopImmediatePropagation()`으로 trailing click을 죽이면서 같은 `document`의 뒤 리스너인 `clearLp`까지 차단** → 두 번째 탭의 롱프레스 타이머가 안 꺼지고 ~550ms 뒤 메뉴가 떴음(±10초 시킹하려고 더블탭했는데 외부플레이어 팝업). 이제 이 네 리스너를 **`window` 캡처**로 옮김 — 캡처 단계는 `window`→`document` 순이라 `window` 리스너가 항상 먼저 실행돼 `document`의 `stopImmediatePropagation`에 안 막히고 더블탭이 롱프레스를 정상 취소.
  - **스트림 감지 범위 확대 (v1.3.45):** 예전 `VideoStreamSniffer`는 쿼리를 떼어낸 경로가 `.m3u8`/`.mp4`/`.webm`로 **끝날 때만** 잡아, 확장자가 URL 중간·쿼리 파라미터에 있는 스트림(`/play?file=video.mp4`, `/hls/index.m3u8/seg?…`)은 놓쳐 "스트림 못 찾음"이 자주 떴음. 이제 `detectMime()`이 (1) 경로 끝 확장자 먼저 확인 후 (2) URL 어디든 확장자 토큰이 **비영숫자 경계**로 등장하면(예: `.mp4a`/`.webmanifest` 오탐 방지) 매칭하는 폴백을 둠. **여전히 `blob:`/MSE/DRM/토큰 스트림은 평문 URL이 없어 추출 불가** — 이 경우는 버튼이 안 뜨고 메뉴도 "스트림 못 찾음" 토스트. **헤더 주입이 핵심** — `DefaultHttpDataSource.Factory`에 `Referer`/`Cookie`/`User-Agent`를 실어 보호된 CDN(토큰 없는 서명·핫링크 차단)도 WebView와 동일 컨텍스트로 받아옴. ExoPlayer는 `DefaultMediaSourceFactory`(media3-exoplayer-hls 포함이라 HLS 자동 처리) + `seekBack/ForwardIncrementMs=10s`. UI는 media3 `PlayerView` 기본 컨트롤러(재생/정지·스크러버·±10초 버튼) + `overlayFrameLayout`에 얹은 제스처 뷰(컨트롤러보다 아래라 컨트롤러 표시 중엔 버튼이 우선) — **단일탭=컨트롤러 토글, 좌/우 더블탭=∓/±10초, 중앙 더블탭=재생정지, 세로 드래그=좌 밝기/우 볼륨**(피드백 토스트형 오버레이). 영상 비율로 자동 회전(`onVideoSizeChanged`). 이어보기는 **`WatchProgressStore`를 페이지 URL 키로 공유**해 WebView↔플레이어 위치 연속(같은 `ResumeSwitch` 토글, 끝까지 보면 삭제). **한계:** `blob:`/MSE/DRM/토큰 스트림은 평문 URL로 추출 불가 → 그런 페이지는 메뉴가 "스트림 못 찾음" 토스트를 띄우고 WebView 재생 유지. 스니퍼는 서브리소스 GET을 봐야 하므로 **영상을 잠깐 재생해 `.m3u8`/`.mp4`가 요청된 뒤** 메뉴가 동작.
- **영상 오프라인 다운로드 (`player/DownloadCenter` + `VideoDownloadService`, v1.3.74):** "통신 상태 때문에 재생이 끊긴다"에 대한 진짜 해법 — 미리 받아두면 재생 중에 네트워크를 아예 안 탄다. **파일 저장이 아니라 Media3의 `DownloadManager`를 쓰는 이유**는 스니핑되는 스트림이 대부분 **HLS(`.m3u8`)**, 즉 재생목록 + 조각 파일 수백 개라 "그 파일을 받는다"로 표현이 안 되기 때문이다(`media3-exoplayer-hls`가 이미 의존성에 있어 다운로더가 딸려 온다 — **새 Gradle 의존성 0개**). 다운로드 저장소와 재생 캐시가 **같은 `SimpleCache`** 라, 다 받은 영상은 재생 시 그 URI에 대한 캐시 히트가 되어 그대로 디스크에서 읽힌다. 설계상 반드시 지켜야 하는 것 셋 —
  1. **`NoOpCacheEvictor` 필수.** 축출하는 캐시를 쓰면 자리를 만들려고 *다 받은 영상을 조용히 지운다*. 다운로드가 절대 해서는 안 되는 단 하나의 일이다. 그래서 이 캐시는 스스로 안 줄어들고, 사용자가 목록에서 지울 때만 줄어든다.
  2. **그렇기 때문에 재생 경로의 캐시는 읽기 전용.** `playbackDataSourceFactory`가 `setCacheWriteDataSinkFactory(null)` + `FLAG_IGNORE_CACHE_ON_ERROR`로 붙는다 — 안 그러면 그냥 스트리밍한 것까지 캐시에 쌓여 무한히 커지고 "받은 것"과 "어쩌다 캐시된 것"의 경계가 흐려진다.
  3. **요청마다 헤더 재부착.** 이 CDN들은 Referer/Cookie를 보는데 `DownloadManager`는 전역 `DataSource.Factory` 하나만 갖고, HLS에서는 **재생목록 URI가 아니라 조각 URI만** 본다. 그래서 `ResolvingDataSource`가 매 요청의 `DataSpec`에 **호스트로 찾은** Referer/UA(`SharedPreferences("video_downloads")` JSON — Room 회피, `TabPersistence` 패턴)를 얹고, Cookie는 `CookieManager`에서 **그때그때 읽어** 세션이 갱신돼도 도중에 상하지 않게 한다. 조각이 형제 CDN 호스트에 있을 수 있어 호스트 미스 시엔 가장 최근 엔트리로 폴백.
  진입점은 영상 롱프레스 메뉴의 "다운로드 (다 받고 보기)"/"받으면서 바로 보기"와 ⋮ 메뉴 "영상 다운로드"(모두 `BrowserScreen`의 공용 `downloadCandidate(candidate, watchNow)` 람다 — `playCandidate`와 같은 후보 해석), 목록은 ⋮ → "다운로드 목록"(`DownloadsScreen`). `.m3u8`만 `DownloadHelper`를 거쳐 화질 하나를 고르고(마스터 재생목록의 모든 비트레이트를 받지 않도록), 단일 파일은 곧장 `DownloadRequest`. **서비스는 포그라운드**(`dataSync` + `FOREGROUND_SERVICE_DATA_SYNC`)라 브라우저 화면을 떠나도 계속 받는다. `getScheduler()`는 `null` — `PlatformScheduler`를 쓰면 앱을 켜지 않아도 재부팅 후 자동 재개가 되지만 `RECEIVE_BOOT_COMPLETED`가 필요해 안 쓴다(대가와 보완책은 아래 v1.3.76 메모). 알림은 Media3의 `DownloadNotificationHelper` 대신 `NotificationCompat`으로 직접 만든다(로컬 컴파일 검증이 없는 저장소라 확인 못 한 패키지명에 의존하지 않기 위해). `POST_NOTIFICATIONS`는 첫 다운로드 때 best-effort로 요청 — 거부돼도 다운로드는 그대로 돌고 진행률 알림만 안 보인다. **한계:** 토큰/DRM으로 보호된 스트림은 재생목록을 못 읽어 실패한다(그 경우 "보호된 스트림일 수 있어요"로 알림).
- **껐다 켜면 이어받는다 (`resumeInterrupted`, v1.3.76):** 받던 도중 앱이 죽거나 재부팅되면 **바이트와 큐는 살아남는데 다시 돌지를 않았다.** 받아둔 조각은 `SimpleCache` 에, 큐는 Media3 `downloadIndex`(SQLite)에 영속화되므로 처음부터 다시 받는 일은 원래 없다 — 문제는 **다시 시작해주는 주체가 없다**는 것이었다. `getScheduler()` 가 `null` 이라(위 참조) 아무것도 자동 재개를 걸지 않고, `PlayerBrowserApp.onCreate` 도 `DownloadCenter` 를 건드리지 않는다. 그래서 앱을 켜도 큐는 가만히 있다가 **사용자가 다운로드 목록 화면을 열어야** 비로소 `DownloadCenter.downloads()` → `manager()` 로 `DownloadManager` 가 만들어지고, 그 생성자가 인덱스를 읽으며 뒤늦게 재개했다. 게다가 그 재개는 **포그라운드 서비스 없이** 앱 프로세스에서 도는 모양이라 그대로 백그라운드로 보내면 또 죽고 진행률 알림도 안 떴다. 이제 `MainActivity.onStart` 가 `DownloadCenter.resumeInterrupted(this)` 를 부른다 — 백그라운드 스레드에서 인덱스의 `STATE_QUEUED`/`STATE_DOWNLOADING`/`STATE_RESTARTING` 개수만 세고, **0이면 아무것도 하지 않으며**(다운로드가 없는 사용자는 서비스도 알림도 안 뜬다) 하나라도 있으면 `DownloadService.sendResumeDownloads(foreground = true)` 로 포그라운드 서비스를 올려 제대로 이어받는다. **`onStart` 인 이유:** Android 12+ 는 백그라운드에서 시작하는 포그라운드 서비스를 거부하므로 앱이 확실히 전면에 있는 지점이어야 한다(그래도 스레드가 늦게 돌 수 있어 `runCatching` 으로 감싼다 — 거부돼도 종전처럼 목록을 열면 재개된다). **일시정지한 항목은 건드리지 않는다** — 사용자 일시정지는 per-download `stopReason` 이고 `sendResumeDownloads` 는 그걸 지우지 않으며, 애초에 `STATE_STOPPED` 라 위 카운트에도 안 잡힌다. **남은 한계:** 앱을 열지 않으면 여전히 재개되지 않는다(그게 `PlatformScheduler` 를 안 쓴 대가다).
- **받으면서 바로 보기 (v1.3.75):** 다운로드는 다 받아야 쓸모가 있는 게 아니다 — `SimpleCache` 는 **쓰는 중에도 읽을 수 있다**. `playbackDataSourceFactory` 는 `FLAG_BLOCK_ON_CACHE` 없이 붙으므로 `CacheDataSource` 가 `startReadWriteNonBlocking` 을 호출하고, 다운로더가 쓰고 있는 구간은 락이 걸려 null 이 돌아오며 그 구간만 조용히 업스트림(네트워크)으로 흘러간다 — 블로킹도 데드락도 없다. 즉 **데이터 계층은 처음부터 동시 읽기/쓰기를 지원했고 막고 있던 건 UI 하나뿐**이었다(`DownloadsScreen` 의 `clickable(enabled = status == COMPLETED)`). 이제 `entry.playable`(= `REMOVING`/`FAILED` 가 아닌 전부)로 풀고, 진행 중 항목은 "지금 보기"(`PlayCircle`)와 다운로드 제어(`Pause`/`Download`) 버튼을 **따로** 갖는다(보는 것과 받는 것은 별개의 일이다). 진입점은 영상 롱프레스 메뉴의 3번째 항목 "받으면서 바로 보기" — `downloadCandidate(candidate, watchNow = true)`.
  - **HLS 는 스트림 키가 없으면 헛돈다.** `DownloadHelper` 는 `setForceHighestSupportedBitrate(true)` 로 **화질 하나를 골라** 받는데, 재생은 적응형이라 낮은 비트레이트에서 램프업한다. 그냥 URL 로 재생하면 다운로더가 캐시한 것과 **다른 렌디션**의 조각을 요구해 디스크에 있는 바이트가 통째로 무시되고 같은 영상을 두 번 받는다. 그래서 `DownloadCenter.mediaItemFor(url)` 이 **그 다운로드의 `DownloadRequest.toMediaItem()`** 을 돌려주고(= `streamKeys` 포함) `VideoPlayerActivity.buildPlayer` 가 URL 로 `MediaItem` 을 짓는 대신 그걸 그대로 쓴다. 다운로드가 없는 URL 이면 null → 종전대로 자기가 만든다.
  - **큐잉과 인덱스 사이의 틈(`pendingRequests`).** `DownloadService.sendAddDownload` 는 인텐트 왕복이라 `onResult(true, …)` 직후에도 `downloadIndex.getDownload(url)` 이 아직 null 일 수 있다 — 그 한 박자 때문에 "받으면서 바로 보기" 의 **첫 재생만** 스트림 키를 잃으면 위의 렌디션 불일치가 그대로 재현된다. 그래서 `send()` 가 성공 시 `DownloadRequest` 를 메모리 맵에 남기고 `mediaItemFor` 가 인덱스 → 펜딩 순으로 본다(인덱스에 뜨면 펜딩 엔트리 제거, 16개 넘으면 통째로 비움). 재생 자체는 `enqueue` 의 **콜백에서** 띄운다 — HLS 는 `DownloadHelper.prepare()` 가 비동기라 콜백 이전엔 `DownloadRequest` 가 존재조차 안 한다. 큐잉 실패/중복이어도 재생은 띄운다(볼 수 있는 걸 안 틀어줄 이유가 없다).
  - **목록에서 트는 항목은 헤더를 다시 실어야 한다.** 아직 안 받은 뒷부분은 여전히 네트워크로 나가는데 그 CDN 들은 Referer/UA 를 본다. `DownloadCenter.rememberedHeaders(url)` 가 큐잉 때 저장해둔 Referer/UA 를 돌려주고(원래 탭은 이미 사라졌을 수 있다) Cookie 만 `CookieManager` 에서 그때그때 읽는다 — 세션이 갱신됐으면 새 값이 쓰인다.
  - **트레이드오프(솔직히):** 보면서 받으면 앞부분은 재생과 다운로드가 **같은 구간을 두 번** 받는다. 회선이 아주 느리면 다 받고 보는 쪽보다 오히려 더 끊긴다. 다운로더가 앞서 나가고 나면 사라지는 문제이지만, "언제나 이쪽이 낫다" 는 아니다.
- **버퍼 확대 (`DefaultLoadControl`, v1.3.74):** 다운로드하지 않고 그냥 볼 때의 끊김을 줄이는 보완책. Media3 기본값(min/max 50s, 재생시작 2.5s, 리버퍼 후 5s)에서 **min 30s / max 120s** 로 올리고 `setPrioritizeTimeOverSizeThresholds(true)`로 바이트 임계값이 시간 버퍼를 잘라먹지 않게 한다. 제약: `minBufferMs >= bufferForPlaybackAfterRebufferMs`, `maxBufferMs >= minBufferMs` 를 어기면 빌더가 던진다.
- **풀스크린 동영상 (`GestureCapturingFrame`, v1.3.42 임시 수리):** `WebChromeClient.onShowCustomView`에서 `GestureCapturingFrame`(FrameLayout)으로 감싸 제스처 캡처 + 화면 회전/풀스크린/세로 드래그 처리. `onHideCustomView`에서 brightness 복원. **이전 v1.3.39 "raw 터치 완전 소비(`return true`) + `window.__pb.*` 훅만 구동" 방식은 실제 `<video>`가 cross-origin iframe 안에 있을 때 최상위 훅이 비디오를 못 찾아 터치가 통째로 먹통**이 됐음(사용자 피드백 "풀스크린 터치 자체가 안 먹힘"). 그래서 임시 수리로 **raw 터치를 다시 사이트(WebView)로 포워드(`super.dispatchTouchEvent`)** 해 사이트 자체 컨트롤/탭/더블탭·네이티브 `<video controls>` 바가 정상 동작하게 하고, 앱은 사이트가 절대 제공 안 하는 **세로 밝기/볼륨 + 2손가락 영상전환** 두 제스처만 얹음(엔게이지 순간 자식에 `ACTION_CANCEL` 보내 사이트의 같은 드래그 추적을 끊음). `window.__pb.fsActive=true`로 인-도큐먼트 `video_gestures.js`는 풀스크린에서 여전히 bail(이중 처리 방지). 앱 풀세트 제스처(±10초 더블탭 등)는 위 **내장 Media3 플레이어**가 담당 — 이 프레임은 어디까지나 사이트 네이티브 컨트롤을 되살리는 임시책. (`fsTap`/`fsDoubleTap`/`scrub*` JS 훅은 남아 있으나 Kotlin이 더 이상 호출 안 함)
- **동영상 이어보기 (resume):** 페이지별 마지막 재생 위치를 기록했다가 다음 방문 시 그 지점부터 재생. JS(`video_gestures.js`의 `initResume`) ↔ Kotlin(`ResumeBridge`, `window.PBResume`) 브리지 구조. JS가 비디오 이벤트(`pause`/`seeked`/`ended`)와 5초 인터벌·`visibilitychange`/`pagehide`에 `bridge.save(pos, dur, title)` 호출, `loadedmetadata`/`play` 시 `bridge.load()`로 저장 위치를 받아 `currentTime` 세팅(`v.__pbResumed` 가드로 비디오당 1회). 너무 짧은 영상/시작·끝 근처는 제외(`MIN_DURATION_SEC=90`, `MIN_RESUME_SEC=10`, `END_GUARD_SEC=20`), 끝까지 보면 `ended`에서 기록 삭제. 저장소는 `WatchProgressStore`(`data/`) — Room이 아니라 SharedPreferences JSON 맵(키=프래그먼트 제거한 정규화 URL). Room의 `fallbackToDestructiveMigration()`이 버전 bump 시 즐겨찾기/방문기록을 날리므로 테이블 추가 대신 `TabPersistence`와 같은 SharedPreferences 패턴 채택. 최대 500개 prune. `currentUrl`은 `onPageStarted`/`onPageFinished`에서 `ResumeBridge`에 주입 (JS는 자신의 페이지 URL을 모르므로 Kotlin이 컨텍스트 제공). 설정 토글 → `ResumeSwitch.enabled` volatile 플래그 (브리지 hot path), `WatchProgressStore`에 영향 없이 기록/복원만 무력화.
- **크래시 로깅:** `PlayerBrowserApp.onCreate` 가장 먼저 `CrashRecorder.install(this)`. `Thread.setDefaultUncaughtExceptionHandler` 체이닝으로 시스템 다이얼로그는 그대로 유지. 추가로 `WebViewClient.onRenderProcessGone`이 `true` 반환해 앱 살아남기 + 에러 페이지 + `CrashRecorder.record()`.
- **자체 업데이트:** GitHub Releases API 조회 → APK 다운로드 → **`PackageInstaller` 세션으로 앱이 직접 설치**(v1.3.66). 예전엔 `FileProvider` + `ACTION_VIEW` 로 APK 파일을 시스템 "패키지 설치 관리자" 앱에 넘겼는데, 그러면 설치를 남이 대신 해주는 모양이 되고 인텐트를 받을 수 있는 앱이 여럿이면 선택 창까지 뜨며 **성공/실패를 우리가 전혀 모른다**. 이제 `UpdateInstaller.install()` 이 세션을 만들어 APK 바이트를 직접 써넣고 `commit()` 하므로 파일이 앱 밖으로 나가지 않고 결과가 `UpdateInstallReceiver` 로 돌아온다(실패 시 이유를 토스트+`DebugLog`). **완전 무음 설치는 일반 앱에 허용되지 않는다** — 시스템 확인 창은 OS 보안 모델이라 우회 대상이 아니다. 다만 Android 12+ 에서 *자기 자신 업데이트*이고 현재 설치된 앱을 심은 installer 가 우리일 때는 `setRequireUserAction(USER_ACTION_NOT_REQUIRED)` 가 받아들여져 확인 없이 설치되므로, **이 경로로 한 번 설치되고 나면 그다음 업데이트부터 확인 창이 사라질 수 있다**(조건이 안 맞으면 시스템이 `STATUS_PENDING_USER_ACTION` 을 돌려주고 리시버가 확인 창을 띄운다 — 어느 쪽이든 안전). 세션 생성/쓰기가 실패하면 종전 인텐트 방식(`launchInstaller`)으로 폴백해 업데이트 경로 자체가 막히지 않게 한다. `REQUEST_INSTALL_PACKAGES` 미허용이면 종전대로 설정 화면으로 안내.

## 동영상 제스처 (`video_gestures.js`)

- **싱글탭 → 사이트 컨트롤 레이어 표시 (앱이 탭을 가로채지 않음, v1.3.41):** 단일 탭은 우리가 재생/정지하지 않고 **사이트 플레이어에 그대로 넘겨** 사이트가 자기 컨트롤 레이어(재생바·버튼)를 뜨우게 함 — 예전엔 싱글탭=재생/정지로 앱이 소유하면서 사이트의 "탭하면 컨트롤 표시"와 겹쳐 쓰기 불편했음(사용자 피드백). in-document 경로는 탭이면 `suppressClick`/`stopPropagation`을 **걸지 않고** `lastTap`만 기록(더블탭 판정용) → 진짜 click이 사이트에 도달. `singleTapTimer`/지연 토글 메커니즘 제거. **재생/정지는 중앙 더블탭으로** 이동. (더블탭은 종전대로 두 번째 탭의 trailing click을 죽여 사이트 풀스크린토글 등과 충돌 방지)
- **사이트 오버레이 동기화 (`playPauseAtPoint`):** **중앙 더블탭** 재생/정지는 비디오 API를 직접 부르지 않고 `playPauseAtPoint(video,x,y)`를 거침. 탭 지점 최상위 요소가 **비디오 위에 따로 얹힌 레이어**(사이트 자체 재생버튼·포스터·툴바 등)면 그 요소에 합성 pointer/mouse `click`(`dispatchSyntheticClick`, `isTrusted=false`)을 dispatch해 **사이트가 직접 처리(재생/정지·⏩·확대 등) + 자기 오버레이를 갱신/숨기게** 함 — 안 그러면 API로만 재생돼 영상은 나오는데 사이트 버튼이 안 사라짐. 합성 클릭은 `isTrusted===false`라 capture click-suppressor가 통과시킴. **폴백은 `paused` 상태 변화가 아니라 요소의 인터랙티브 여부로 판정** — `looksInteractive(top)`이면(버튼/cursor:pointer 등) 사이트에 맡기고 우리가 토글하지 않음(확대 버튼처럼 재생과 무관한 동작을 눌렀을 때 직후 영상이 멈추던 버그 수정), 비인터랙티브 오버레이(포스터/그라데이션)거나 dispatch 실패면 `togglePlay` API 폴백. 탭 지점이 비디오 자신이면 곧장 API.
- **비디오 위 컨트롤 통과 (control passthrough):** 비디오 박스 안이라도 탭 지점의 최상위 엘리먼트가 *작은* 인터랙티브 컨트롤(닫기 ×, 사이트 자체 툴바의 재생/정지/⏪/⏩/확대 버튼 등)이면 탭을 가로채지 않고 그대로 통과시켜 사용자가 누를 수 있게 함. `controlAtPoint(x,y,video)`가 `deepElementFromPoint`(shadow DOM 관통)로 최상위 요소를 찾아 `looksInteractive`(button/a/input/role=button/onclick **또는 `cursor:pointer`** — 커스텀 툴바 버튼이 `<div>/<span>/<i>`라도 잡힘)인지 + 그 bounds 면적이 비디오 면적의 절반 미만인지로 판정 → 참이면 touchstart에서 `onVideo=false`로 떨궈 off-video 탭과 동일 취급(hijack/suppressClick 안 함). **전체 비디오를 덮는 click-catcher(재생/정지 오버레이)는 면적 ≥ 절반이라 통과 대상이 아님** → 맨 비디오 탭의 재생/정지는 기존대로 앱이 소유(Option A 유지).
- 1 손가락 좌/우 스와이프 → -10/+10초 시킹
- 2 손가락 좌/우 스와이프 → 이전/다음 `<video>` 전환
- 양쪽 가장자리 더블탭 → 사이드 인식 더블탭 시킹 (좌 -10, **중앙 재생/정지**, 우 +10). 첫 탭은 사이트로 통과(컨트롤 레이어 표시)되고, 두 번째 탭이 오면 더블탭으로 인식해 시킹/재생정지를 수행 + 두 번째 탭의 trailing click을 죽임.
- 수평 드래그 → 정밀 시킹 (드래그 양에 비례). **드래그/스와이프/시킹 등 탭이 아닌 제스처는 끝날 때 합성 `click`을 `suppressClick`으로 같이 죽여** 사이트가 그 클릭으로 재생/정지 토글하거나 컨트롤 오버레이를 띄워 안 사라지는 일을 막음. 또 scrub로 인식되지 못한 작은/세로 드래그(`s.moved`)는 탭이 아니므로 재생/정지를 발화하지 않고 클릭만 죽이고 종료.
- 풀스크린 세로 드래그 → 좌측 화면밝기 / 우측 시스템 볼륨 (`window.__pb.showVbOverlay` 호출)
- **⚠️ 아래 v1.3.39~41 "단일 제스처 소스" 설계는 v1.3.42에서 되돌림(SUPERSEDED).** cross-origin iframe 비디오에서 최상위 `window.__pb.*` 훅이 비디오를 못 찾아 풀스크린 터치가 통째로 먹통이 됐기 때문(위 "풀스크린 동영상" 메모 참조). 현재는 raw 터치를 사이트로 다시 포워드하고 앱은 밝기/볼륨·2손가락 전환만 얹으며, 앱 풀세트 제스처는 **내장 Media3 플레이어**가 담당. 아래 설명은 그 변경 전의 역사적 맥락으로 남겨둠.
- **풀스크린(Kotlin `GestureCapturingFrame`)은 단일 제스처 소스 — raw 터치를 자식에 전혀 안 흘림(v1.3.39, ~v1.3.41까지):** `dispatchTouchEvent`가 모든 터치를 **완전 소비(`return true`)** 하고 `super.dispatchTouchEvent`로 자식 WebView/네이티브 `<video>`에 절대 전달하지 않음. 이전에는 탭 `ACTION_UP`만 `ACTION_CANCEL`로 치환하고 DOWN/MOVE는 그대로 흘렸는데, 그러면 사이트 플레이어가 그 `touchstart`들로 **자기 더블탭/스크럽을 함께 인식**해 우리 제스처와 충돌(이중 시킹·예기치 않은 풀스크린 토글) — 더블탭 판정 기준(click vs touchstart vs pointer)이 사이트마다 달라서 어떤 사이트는 멀쩡하고 어떤 사이트는 깨지던 "균일하지 않음"의 원인. 이제 모든 의도는 `window.__pb.*` 훅으로만 구동: 탭/더블탭은 `GestureDetector.onSingleTapConfirmed`/`onDoubleTap`, 스크럽/볼륨/밝기/`switchVideo`는 `dispatchTouchEvent` 분기. **단일탭은 사이트 컨트롤 레이어 표시(v1.3.41):** 풀스크린은 raw 터치를 완전 소비해 사이트가 탭을 못 보므로, `onSingleTapConfirmed`이 `window.__pb.fsTap(xRatio,yRatio)`로 위임 → JS `runFsGesture({kind:'tap'})`이 탭 지점 최상위 요소(`deepElementFromPoint`)에 **합성 클릭만** dispatch(재생/정지 토글 안 함)해서 사이트가 자기 컨트롤 레이어를 뜨우게 함. 재생/정지는 **중앙 더블탭**(`fsDoubleTap`)이 `playPauseAtPoint`로 처리. JS in-document 경로는 `fsActive`로 이미 꺼져 있음.
  - **더블탭 ±10초 구역은 화면이 아니라 *영상 박스* 기준으로 JS에서 판정(v1.3.40):** 이전 `onDoubleTap`은 Kotlin에서 `e.x / screenWidth`로 좌/중/우 1/3을 갈랐는데, 영상이 화면을 안 채우면(letterbox·세로 영상) 화면 1/3과 영상이 안 맞아 대부분 중앙(재생/정지)으로 떨어져 ±10초가 거의 안 걸렸음(비-전체화면 JS 경로는 멀쩡 → "전체화면만 안 됨"의 원인). 이제 Kotlin은 탭 위치 0~1 비율만 넘기고, **`window.__pb.fsDoubleTap`** 이 in-document 더블탭과 **동일한 로직**(`v.getBoundingClientRect()` 기준 `rel<0.35`/`>0.65`)으로 좌(−10)/중(재생정지)/우(+10) 판정 — 두 경로 일원화. Kotlin `SEEK_SECONDS` 상수 제거.
  - **iframe-호스트 플레이어 릴레이:** 모든 discrete 훅(`seek`/`fsTap`/`fsDoubleTap`/`togglePlay`/`switchVideo`)을 JS `runFsGesture(p)`로 통일 — 이 프레임에 비디오가 없으면(`fullscreenVideo()||activeVideo()` 둘 다 null) 자식 iframe에 `postMessage({__pbFs:p})` **브로드캐스트**. 각 프레임이 같은 스크립트의 `message` 리스너로 받아 자기 비디오에 실행(없으면 더 깊이 재릴레이). evaluateJavascript는 최상위 프레임에서만 실행되는데 실제 `<video>`는 (cross-origin) iframe 플레이어 안에 있는 경우가 많아 최상위 훅이 비디오를 못 찾던 문제 보완. 비디오가 있는 프레임만 동작해 **이중 처리 없음**. 각 훅은 진단용 상태 문자열(`seek-`/`seek+`/`pp`/`relay`…)을 반환 → Kotlin이 `DebugLog.d("FsGesture", …)`로 기록(설정→디버그 로그에서 더블탭 발화/비디오 매칭 확인 가능). **남은 한계:** 스크럽(`scrubStart/By/End`)은 아직 최상위 프레임 비디오만 대상(stateful이라 미릴레이) — cross-origin iframe 영상은 풀스크린 스크럽 미동작.
  - **영상 비율 따라 화면 자동 회전(개선, v1.3.40):** `onShowCustomView`가 `DETECT_VIDEO_ORIENTATION_JS`로 `videoWidth>=videoHeight`면 가로(`SENSOR_LANDSCAPE`)·아니면 세로(`SENSOR_PORTRAIT`)로 회전. 풀스크린 진입 시점엔 영상 intrinsic 크기가 아직 0일 수 있어 이전엔 세로 영상도 가로로 굳던 경우가 있었음 — 이제 JS가 크기 미확정 시 `'unknown'` 반환, Kotlin `applyVideoOrientation(attempt)`이 200ms 간격 최대 8회 재프로브(그 동안 가로 기본 유지)해서 메타데이터 로드 후 올바른 방향 적용.
  - **트레이드오프:** 네이티브 `<video controls>`의 기본 컨트롤 바 자동 표시/네이티브 스크러버 드래그는 raw 터치를 못 받아 안 뜸 — 우리 스크럽 제스처가 대체. **한계:** 이 단일소스화는 `onShowCustomView`를 거치는 *네이티브* 풀스크린에만 적용 — 사이트가 CSS로 뷰포트를 채우는 *인-페이지* 풀스크린은 `fsActive=false`라 in-document(passive) 경로라 사이트 터치를 완전히 막지 못함(별도 작업 필요).
- **이어보기:** `initResume` 모듈이 비디오 위치를 `window.PBResume` 브리지로 저장/복원 (위 "동영상 이어보기" 메모 참조). 90초 이상 영상만 대상, 진입 시 토스트로 안내. 설정에서 토글.
- iframe 내부: `IframeScriptInjector`가 cross-origin iframe HTML에도 주입

수정 후에는 실제 페이지(YouTube, 일반 `<video>` 사이트)에서 테스트 필요. JS는 페이지마다 다른 player 구현과 부딪힐 수 있음.

## 종속성 메모

- Compose BOM `2024.06.00`
- Room `2.6.1` (KSP)
- androidx.appcompat `1.7.0` (MediaRouteButton 호환용)
- androidx.mediarouter `1.7.0` + play-services-cast-framework `21.5.0`
- androidx.webkit `1.11.0`
- androidx.media3 `1.4.1` (media3-exoplayer / media3-exoplayer-hls / media3-ui — 내장 네이티브 플레이어)
- OkHttp `4.12.0` (SNI 우회 / 업데이트 클라이언트)
- DataStore Preferences `1.1.1`

## 검증 / 수동 테스트

- **테스트 인프라 없음** — 단위/E2E 테스트 디렉터리가 비어있음. 변경사항은 실제 기기(S24가 타깃)에서 빌드/설치 후 확인이 기본 검증
- 빌드 검증: `./gradlew assembleDebug` 성공 여부 (로컬 wrapper 없으면 CI가 PR/푸시에서 검증)
- 기능 검증은 사용자가 S24에 APK 설치 후 직접 진행
- 충돌 진단: 메뉴 → 설정 → 디버그 로그 상단의 "충돌 기록" 섹션에서 스택트레이스 확인/복사 (`filesDir/crashes/`에 영구 저장)

## 작업 시 주의사항

- **버전 bump:** 기능 추가/수정 commit과 함께 `versionCode`/`versionName` 증가 — 자체 업데이트 동작에 필수
- **commit 메시지 컨벤션:** `feat(scope):` / `fix(scope):` 형태. scope는 `ui`, `player`, `sni`, `adblock`, `cookies`, `cast`, `tabs`, `debug` 등
- **태그 푸시:** `v1.3.x` 태그 push로 GitHub Release + APK 자동 발행. CI가 오래된 릴리스 자동 정리 (최신 3개 유지)
- **문서 동기화:** 기능 추가/제거 시 `README.md`의 "주요 기능" 섹션과 본 파일의 "디렉터리 구조" / "핵심 아키텍처 메모"를 같은 commit에 묶기
- **JS 자산 변경:** `app/src/main/assets/video_gestures.js`는 WebView 컨텍스트에서 실행 — `console.log`는 Chrome devtools 또는 `chrome://inspect`에서만 보임
- **SNI 우회 작업:** `DebugLog` + `DebugLogScreen`으로 인앱 진단 가능. KT/SKT의 DPI 정책이 자주 바뀌므로 fragmentation 파라미터 조정이 필요할 수 있음
- **광고 차단 작업:** 새 광고 도메인은 `AdBlocker.BLOCKED_HOSTS`에 host suffix로 추가 (substring이 아니라 endsWith `.entry` 매칭이므로 false positive 위험 낮음). 사이트가 광고차단 감지로 콘텐츠를 막으면 설정 토글로 우회.
- **쿠키 배너 작업:** 새 컨센트 플랫폼 셀렉터는 `CookieBannerKiller.SCRIPT`의 `REJECT_SELECTORS` / `HIDE_SELECTORS`에 추가. CSS 와일드카드 셀렉터(`.sp_message_container_*`)는 `document.querySelector`가 throw할 수 있어 try/catch로 감싸져 있음 — 새 셀렉터도 같은 패턴 유지. 사이트가 배너 동작에 깊게 의존(예: 동의 후에만 video 로드)하면 설정 토글로 우회.
- **탭 그룹 / 부모 관계 작업:** `TabState.parentTabId`/`groupId`와 `TabGroup`은 `TabPersistence` JSON에 함께 저장 — 스키마 추가 시 `optString().ifBlank { null }` 패턴으로 backward-compatible 로드 유지. 그룹 삭제 시 탭은 살아남고 `groupId`만 null로 떨어지는 정책은 사용자 실수 보호용이므로 임의로 바꾸지 말 것. `closeTabs`의 orphan sanitization(부모가 닫힌 자식의 `parentTabId` null화)은 댕글링 BackHandler 방지에 필수.
- **User-Agent를 임의로 건드리지 말 것:** UA는 `UserAgentSpoof.chromeLike()` 한 곳에서만 만든다. 접미사를 붙이거나 데스크톱 UA로 바꾸면 UA와 실제 TLS/UA-CH 지문이 어긋나 안티봇 점수가 오히려 나빠지고, 과거엔 `ERR_CONNECTION_RESET`까지 났다. 허용되는 방향은 **표식 제거(비표준 → 표준)** 뿐.
- **캡차/안티봇 챌린지는 절대 가로채지 말 것:** 챌린지를 쓰는 호스트는 `ChallengeDetector`의 격리 셋에 들어가 **문서·서브리소스·iframe 전부** 네이티브 전담이 된다(v1.3.56) — `SniBypassClient`/`IframeScriptInjector`는 격리 여부만 물어보고 자기 예외를 만들지 않는다. 새 안티봇 서비스가 나오면 `ChallengeDetector.CHALLENGE_HOSTS`(호스트 suffix) / `CHALLENGE_PATHS`(경로 substring)에 추가만 하고, `IframeScriptInjector`·`SniBypassClient`에 예외를 따로 만들지 말 것 — 두 곳 모두 이 한 판별기를 본다. `shouldInterceptRequest`가 POST 본문을 볼 수 없어 "GET만 가로채기"는 챌린지 흐름을 반드시 반으로 쪼갠다. 경로 목록에 `/cdn-cgi/`를 통째로 넣으면 `/cdn-cgi/image/`(콘텐츠 이미지)까지 네이티브로 빠져 차단 사이트 이미지가 깨지므로 챌린지 하위 경로만 유지할 것.
- **인터셉트 체인 순서:** `shouldInterceptRequest`에서 `AdBlocker → VideoStreamSniffer.observe → SniBypassClient.intercept → IframeScriptInjector.process` 순. AdBlock이 가장 먼저 (cheapest, 빈 204로 짧게 끝남).
- **크래시 핸들러 수정 금지:** `CrashRecorder.install`은 기존 시스템 핸들러를 체이닝하므로 절대 끊지 말 것. WebView `onRenderProcessGone`은 반드시 `true` 반환 (false면 앱 종료).
